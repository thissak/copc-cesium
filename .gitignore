node_modules/
dist/
demo-dist/
*.tgz
*.log

# 대용량 COPC 원본은 커밋하지 않는다
data/
*.copc.laz
*.laz
*.las

.DS_Store
.vite/

# Claude Code 로컬 산출물 (워크트리 체크아웃 등) — 커밋 금지
.claude/

# 환경변수 / 시크릿 (npm 토큰 등) — 절대 커밋 금지
.env
.env.*
!.env.example

# docsite (mkdocs) 빌드 산출물
site/

# 벤치 프로브 빌드 산출물 (probe.ts → esbuild IIFE)
scripts/bench/probe-bundle.js

# Playwright MCP 테스트 산출물 + 탐색 스크린샷
.playwright-mcp/
copc-phaseA-demo.jpeg
round1-*.png
cahokia-*.png
smoke-*.png

# 결과보고서 초안 — premature deliverable, 로컬 보존만. T1 정식 착수 시 다듬어 커밋
docs/결과보고서.md

# 출품물 — 대용량 산출물만 제외하고 문서는 추적한다.
# (2026-08-09 판 산출물이 `docs/submission/` 통째 ignore 때문에 전부 유실된 전례가 있다.
#  영상·오디오·프레임은 재생성 가능하지만 결과보고서·대본은 다시 만들기 비싸다.)
docs/submission/video/assets/audio/
docs/submission/video/assets/render/
docs/submission/video/assets/raw/
docs/submission/video/*.mp4
docs/submission/video/*.webm
docs/submission/video/thumbnail*.png

# 로컬 전용 문서·산출물
docs/DIRECTION.md
.vercel
