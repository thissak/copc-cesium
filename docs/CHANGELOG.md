# CHANGELOG

날짜별 변경 내역 + 결정 사유. 최신이 위.

### 2026-08-06
- [fix] **[PR #28 dual-review R12] 공식 LAS extent 신뢰와 축 판정 SSOT 정리.** projected header edge reprojection 실패는 cube 수평 metric으로 복구해 얕은 Z 조기반환을 차단. 정상 LAS header Z는 node cube와 교집합으로 조이고 손상/비교집합만 cube로 복구해 SSE 정확도와 완전포함을 함께 보존. `headerAxisValidity`로 코어·tileset 판정을 통합하고 학습 문서·ADR 충돌을 정리. Blue WARNING 2·MINOR 3 대응. ([ADR-007](adr/007-spatial-reference-metric-separation.md) 보강)
- [fix] **[PR #28 dual-review R11] geographic 퇴화축과 projected Z 완전포함 계약 보존.** geographic 수직 스캔(동일 XY·양수 Z)과 Z 손상·유효 XY를 각각 독립 metric 축으로 인정해 mixed-unit cube 없이도 로드. projected tile Z는 stale header와의 부분/비교집합 여부와 무관하게 node cube로 완전포함해 culling을 차단. 이슈 #23의 8~11차 계약과 테스트 명칭도 현행화. Blue CRITICAL 1·WARNING 2·MINOR 2 대응. ([ADR-007](adr/007-spatial-reference-metric-separation.md) 보강)
- [fix] **[PR #28 dual-review R10 + 공식 규약 재설계] header 축별 신뢰와 COPC cube 불변식으로 metric fallback 단순화.** 반복된 손상 bbox edge-case를 band-aid로 막지 않고 OGC 3D Tiles의 metric·완전포함 계약과 COPC 단일-radius 정육면체 규약으로 재설계. projected는 header XY 유효 시 실측하고 손상 시 `cube side × max(horizontalUnit,zUnit)`로 복구하며, geographic은 mixed-unit cube를 metric 근거로 쓰지 않고 fail-loud. tile Z는 node/header 교집합이 있을 때만 clamp해 stale Z culling을 차단. Red WARNING 1 + Blue WARNING 2·MINOR 1 대응. ([ADR-007](adr/007-spatial-reference-metric-separation.md) 보강)
- [fix] **[PR #28 dual-review R9] 손상 bbox의 projected XY·Z LOD 폴백 통일.** 전축 0 header가 Z만 유효한 것으로 오인돼 1000m content region이 1m 슬래브로 붕괴하던 결함을 header 전체 유효성 계약으로 통일. root span fallback은 projected hierarchy cube의 수평 폭×`horizontalUnit`과 수직 폭×`zUnit` 최대값을 사용해 넓고 얕은 데이터의 refinement를 보존. 학습 문서 3곳의 옛 spacing 공식을 실제 `rootSpanM / 16 / 2^d`로 현행화. Red WARNING 1 + Blue WARNING 1·MINOR 1 대응. ([ADR-007](adr/007-spatial-reference-metric-separation.md) 보강)
- [fix] **[PR #28 dual-review R8] CRS 파서 포맷·단위 SSOT와 projected region 경계 확정.** compound WKT 키워드의 대소문자·괄호 앞 공백을 허용하고, projected 선형단위는 제한된 수기 표 대신 proj4 `to_meter`를 사용해 `km` 등 지원 단위를 보존. header clamp는 geographic에만 한정해 stale header 밖 projected node의 culling을 차단. 학습 문서 높이 코드는 source snippet transclude로 전환. Red WARNING 1 + Blue WARNING 1·MINOR 2 대응, unit 9/9·integration 9/9·build·lib·docs strict PASS. ([ADR-007](adr/007-spatial-reference-metric-separation.md) 보강)
- [fix] **[PR #28 dual-review R7] 퇴화 header bbox의 region 위치 복구.** GE만 cube로 복구하고 region은 `(0,0)` 한 점으로 만들어 projected 데이터가 컬링되던 결함을 CRS별 폴백으로 수정(projected=node cube, geographic=fail-loud). 손상 Z는 node cube Z로 복구하고 정상 header region 샘플은 sub-mm 격자 사용. 1° metric 정밀 단언과 CRS 위키 현행화. Red CRITICAL 1 + Blue WARNING 1·MINOR 4 대응. ([ADR-007](adr/007-spatial-reference-metric-separation.md) 보강)
- [fix] **[PR #28 dual-review R6] ESRI `VERTCS`와 손상 bbox의 root GE 폴백 보강.** 수평 ft·수직 m ESRI compound WKT의 높이 3.28× 축소를 막고, header bbox가 0/비유한일 때 `computeRootSpanM`이 cube 수직 metric span으로 폴백해 SSE=0 정지를 방지. 빈 node/header XY 교집합은 raw geographic cube가 아닌 유효 header bbox 전체를 사용. Blue WARNING 2·MINOR 2 대응. ([ADR-007](adr/007-spatial-reference-metric-separation.md) 보강)
- [fix] **[PR #28 dual-review R5] min-anchored COPC cube sanity와 수직형 LOD 보존.** CRS 중심 가드를 `info.cube`가 아닌 실제 header bbox에 적용해 geographic Z(m) 팽창의 false failure를 제거. root GE는 수평 WGS84 span·수직 미터 span 최대값으로 계산해 동일 XY 보어홀/타워도 SSE=0으로 멈추지 않게 함. 손상 header bbox의 빈 node 교집합은 원래 node 범위로 폴백. 실제 min-anchored cube·200m 수직 scan 계약 추가. Red WARNING 1 + Blue CRITICAL 1·WARNING 2·MINOR 3 대응. ([ADR-007](adr/007-spatial-reference-metric-separation.md) 보강)
- [fix] **[PR #28 dual-review R4] geographic COPC 정육면체 cube의 수평 경계 오염 제거.** COPC `info.cube=center±radius`에서 Z(m)가 X/Y(°)를 수백 도로 팽창시켜 region 생성이 throw하고 GE가 과대 계산되던 결함을 header bbox span + node/header XY 교집합으로 수정. geographic 스냅 metric은 seed ECEF를 루프 밖에 hoist하고 기존 격자 reproject를 사용하며 중복 ECEF 구현을 제거. 실제 cube 규약·grid/fallback 1mm·region clamp 계약 추가. Blue CRITICAL 1·WARNING 1·MINOR 2 대응. ([ADR-007](adr/007-spatial-reference-metric-separation.md) 보강)
- [fix] **[PR #28 dual-review R3] EPSG geographic 별칭의 angular 판정과 실제 세션 metric 계약 보강.** 문자열 스니핑을 proj4 해소 결과(`Proj.projName`)로 교체해 `EPSG:4326`·`WGS84`도 ECEF 스냅 경로로 분류. `sessionMetricMetersSquared`가 projected/source와 geographic/격자-ECEF 분기를 소유하고 unit 테스트가 실제 분기를 통과하도록 변경. README·PROGRESS의 과거 projected-only 한계도 현행화. Red·Blue CRITICAL 공통 1 및 문서/테스트/성능 지적 대응. ([ADR-007](adr/007-spatial-reference-metric-separation.md) 보강)
- [fix] **[PR #28 dual-review R2] geographic snap metric을 WGS84 ECEF 미터 비교로 수정.** geographic compound를 파싱한 뒤 degree를 1m로 오인해 수십 m 수평 후보를 1m 수직 후보보다 가깝게 선택하던 조용한 오답을 `horizontalIsAngular` 분기로 제거. projected 혼합단위는 source metric, geographic은 ECEF metric을 사용하며 실제 미터 거리 제곱 헬퍼를 unit 경계에 포함. 만료 clientId SW 계약도 추가. Red CRITICAL 1 + Blue WARNING 1·MINOR 2 대응. ([ADR-007](adr/007-spatial-reference-metric-separation.md) 보강)
- [fix] **[PR #28 dual-review R1] 혼합단위 snap metric 회귀 및 geographic compound CRS 보강.** `horizontalUnit`/`zUnit`을 세션에 함께 보존하고 Z 차분을 수평 단위계로 정규화해 수평 피트·수직 미터에서도 argmin·`distanceM`을 등방 미터로 유지. compound `GEOGCS`와 proj string `+vunits` 파싱, SW 정상 client 양성 테스트, AGENTS 홈 상대경로를 추가. Blue CRITICAL 1·WARNING 2·MINOR 2 대응, CRS·SW·snap·tsc PASS. ([ADR-007](adr/007-spatial-reference-metric-separation.md) 보강)
- [fix] **[#27] Cesium 최소 호환 버전과 CI 출하 계약 정합화.** npm 배포본 1.120~1.142 대조에서 private empty-tile codec이 1.142부터 존재함을 확인해 peer를 `>=1.142`로 수정. 설치 소스+peer 오프라인 가드와 public type·throttle·SW 검사를 `npm test`에 연결(5→9), CI는 unit 9+integration 9 전체 실행. codec RED→GREEN, unit 9/9·integration 9/9·build·lib·docs strict·pack dry-run PASS. ([ADR-008](adr/008-cesium-compatibility-contract.md))
- [fix] **[#26] hierarchy 서브페이지 동시 로드를 세션 단위 single-flight로 통합.** 같은 key의 진행 중 Promise를 공유해 렌더·스냅 경합 시 동일 range read를 2회→1회로 줄이고, 실패 시 registry만 비워 pointer 기반 재시도를 보존. `check:paging` RED→GREEN, `tsc`·`build:lib` PASS. ([ADR-003](adr/003-hierarchy-subpage-paging.md) 보강)
- [fix] **[#25] Service Worker의 `clientId` 부재 시 임의 탭 라우팅 제거.** `clients.matchAll()[0]`으로 다른 탭을 선택해 로컬 sid 충돌 시 잘못된 COPC를 반환할 수 있던 경로를 차단. 정확한 request client가 없으면 postMessage 0회+503 fail-loud. SW VM RED(`404/posts=1`)→GREEN(`503/posts=0`), `tsc`·`build:lib` PASS. ([ADR-002](adr/002-service-worker-tile-interception.md) 보강)
- [fix] **[#24] Cesium 호스트 요청 제한에 세션 소유권·복원 추가.** `requestsByServer` 공유 static map에 마지막 값을 영구 덮어쓰던 결함을 sid별 acquire/release로 해소. 다중 tileset은 최솟 양수 상한을 공유하고 마지막 destroy/초기화 실패 시 소비자의 기존 값을 복원하며 `0` escape hatch도 실제 해제로 동작. RED→GREEN·`tsc`·공개타입 PASS. ([ADR-004](adr/004-delegate-memory-concurrency-to-cesium.md) 보강)
- [fix] **[#23] 공간참조 수평·수직 단위와 3D Tiles metric 분리.** compound CRS의 수직 unit을 독립 해석하고 root 수평 폭을 WGS84 ECEF chord로 측정해 EPSG:4326·혼합단위에서도 geometric error를 미터로 보장. tile region은 대각선 2점→네 변 8구간 샘플+반자오선 interval로 바꿔 비선형 투영 culling 위험 차단. RED 3건→GREEN, `tsc`·snap·verify PASS. ([ADR-007](adr/007-spatial-reference-metric-separation.md))
- [fix] **[#22] 공개 tileset 타입에 `snapPoint`·`attributeRange` 반영.** 런타임에만 주입되고 `fromUrl(): Promise<Cesium3DTileset>` 선언에서 누락돼 README TypeScript 예제가 TS2339로 실패하던 계약 불일치를 `CopcCesiumTileset` 교차 타입으로 해소. 소비자 컴파일 가드 `check:public-types` 추가, `tsc`·`build:lib` PASS.
- [docs] **Claude Code SSOT→Codex 브릿지 `AGENTS.md` 추가.** 글로벌·프로젝트 `CLAUDE.md`와 `docs/PROGRESS.md`·`docs/CHANGELOG.md` 읽기 순서만 가리키는 얇은 포인터로, 규칙 복제 없이 Claude·Codex가 동일 SSOT를 사용하게 함.

### 2026-07-12
- [docs] **KOSSA 오픈소스 개발자대회 — 참가 접수 완료 + 접수 폼 문구 확정.** 참가신청(~2026-07-17) 마감 전 접수 완료(부문=일반·지정과제 "가이아쓰리디"·단독팀 김대욱). 접수 폼 "프로젝트 정보" 3필드를 결과보고서 기준으로 폼 글자수 제한에 맞춰 확정 — 프로젝트명 `copc-cesium`(폼 금지기호 없음), 개발 목적(194/200자)·프로젝트 소개(294/300자). 소개의 차별화 문구는 "직접 소비하는 **유일한** 오픈소스"→"**드문** 오픈소스"로 톤다운(심사 방어선 확보, "오픈소스 ∩ CesiumJS 빈자리" 근거는 유지). 출품작(결과보고서+소스+3분영상) 제출은 **2026-08-27(D-46) 대기** — moat 보호 위해 그때까지 레포 private·npm publish 보류(DIRECTION §6-A). 확정 문구 verbatim + 기대효과 초안은 DIRECTION §6-A에 보존해 출품작에 재사용. (문서만·코드 무변경)

### 2026-06-25
- [chore] **테스트 진입점 단일화 — `npm test`(오프라인 unit)/`test:integration`(S3 통합)/`test:all` + 집계 러너 `scripts/run-checks.ts`.** **동기**(입상 갭): DIRECTION 스코어카드의 유일한 ❌ — 검증 자산(`verify`+`check-*` 16개)은 있으나 `package.json`엔 2개만 노출되고 `npm test` 진입점이 없어 심사위원 "테스트 없음" 감점 위험. **설계**: 각 스크립트가 이미 `exit 0/1`+PASS/FAIL 규약을 가져 **얇은 집계 러너 1개**로 충분 — vitest 등 프레임워크 도입은 16개 재작성·회귀 위험이라 기각(Simplicity First). 네트워크 의존으로 분리: `test`=PURE 5(crs/ecef/picking/pnts-batch/style, 오프라인 결정적·심사 환경 어디서나 재현), `test:integration`=verify+NET 9(S3 range로 georef/페이징/coalesce/retry/속성/snap e2e). `check-cancel`은 #20 won't-fix 재현 도구라 회귀 가드 아니므로 스위트 제외. 미노출 `check-*`도 개별 `npm run check:<name>`으로 노출(가시성 2→15). **검증(실측)**: unit 5/5·exit 0, 실패 주입 시 exit 1+집계 정확, integration 9/9·exit 0, `npm run build` 회귀 0. 스코어카드 ❌→✅. (`scripts/run-checks.ts`+`package.json`+`README`)
- [fix] **누락된 millsite 기본 URL 정정 — `check-classification`·`check-paging`의 `data.entwine.io`(404)→`hobu-lidar`(206).** 2026-06-18 `datasets.ts` 죽은 URL 정리 때 demo·벤치는 hobu-lidar로 이전됐으나 이 **테스트 2개가 누락**돼 옛 entwine URL을 남겼고, 테스트 진입점 단일화가 즉시 적발(진입점 ROI 실증). 동일 파일 확증(교체 후 `check-paging` "서브페이지 141" PASS). (`scripts/check-classification.ts`+`scripts/check-paging.ts`)

### 2026-06-23
- [feat] **[IMPROVEMENTS Tier1 #3-B] 옥트리 풀해상도 최근접점 스냅 — `tileset.snapPoint()` (PR #21, subagent-driven 6 task TDD + dual-review 2R).** 클릭→`pickPosition` 씨앗→워커가 COPC 옥트리 *가장 깊은 노드*를 온디맨드 디코드→source 공간 *등방* 최근접 실제 점→정확 좌표+LAS 속성+`distanceM` 반환. `pickPosition`(렌더 픽셀 depth)의 빈틈/렌더-LOD/역산 한계를 옥트리(공간 인덱스)로 돌파 — `.pnts` 변환 엔진이 못 하는 구조적 우위. 순수 math(`locateDeepestNode`/`nearestPointInNode`/`nearestPoint`)=copc-core(Cesium-free·Node 결정적), 디코드=워커 재사용, Cesium=page만, **렌더러 손코딩 0·신규 의존성 0**. **dual-review 2R**: R1 — HIGH 비등방 메트릭(`dz`만 ×zUnit → 피트 CRS=autzen 오답)+순환 오라클 → 등방화(전축 raw·dist만 ×zUnit)+독립 WGS84→ECEF 오라클(버그 재주입 FAIL 실증)·hang 타임아웃·CI 배선; R2 — 단일-노드 검색 ≠ 전역 최근접(경계 ~0.14m) → **한계 공개**(README/JSDoc/스펙: 로컬 풀해상도 근사 스냅·이웃검색 후속, over-headline 금지)+계약누수 `.catch`→warn+undefined+N2 등방 변별 테스트; R3 confirming = OK-to-merge. **검증**: `npm run check:snap`(독립 ECEF 오라클·N2)·verify C1 Oregon·check-* 회귀 0·헤드리스 브라우저 스모크·CI 배선. **한계(정직)**: 단일 노드 내 최근접(전역 보장 아님)·투영 CRS 가정·측정도구/시각화=별도 사이클. (스펙/계획 `docs/superpowers/{specs,plans}/2026-06-23-octree-nearest-point-snap*`)
- [decision] **[이슈 #20] 취소/백프레셔 전파 — measure-first 결론 WON'T-FIX (Phase 0 게이트 FAIL).** IMPROVEMENTS Tier2 #4(Codex 발견·giro3d#677) 착수. **재현(RED)**: copc-core range getter 가 외부 취소 신호를 못 받아 in-flight fetch/재시도가 끝까지 도는 것을 결정적으로 박음(`scripts/check-cancel.ts`). **근본원인**: Cesium 은 out-of-view 타일을 `cancelRequests()→xhr.abort()`로 취소하나(1.142 소스 확인), 우리 SW-인터셉트 아키텍처에서 그 신호의 유일 진입점은 SW `event.request.signal`. **BP 조사**(deep-research+context7): comlink out-of-band cancel·AbortSignal.any·p-retry signal·WASM 중단불가(노드경계 체크)·업계 지배패턴=시작-전 스킵. 단 **SW signal 이 Chrome-only·XHR 불확실**이 load-bearing 리스크. **Phase 0 게이트(measure-first, 빌드 전)**: `scripts/bench/probe-sw-cancel.ts`(자가완결 Node http+Playwright)로 실측 → 클라이언트 XHR/fetch abort 시 SW `event.request.signal` **미발화**(2.3s 폴링 불변, w3c/ServiceWorker #1544 정합) = **GATE FAIL** → SW-signal 기반 클린 수정 불가. **실해 측정**(`scripts/bench/repro-20.ts`+진단 훅 `copcDecodeStats`): churn 3패턴서 in-flight 디코드 ≤2(throttle 6 미만)·motion 정지 시 0 수렴, 좀비 무계 누적 미관측(headless 한계로 이론 최악만 미실증·그조차 throttle 유계·self-heal). **판정**: 클린 채널 차단 + 유계 영향 + Tier2 비헤드라인 → won't-fix(fragile Cesium-내부 monkey-patch 만 가능, STOP·회귀위험상 비정당). [[completeness-gaps-vs-perf-measure]]·#14·#19·#05 선례. 재개=Cesium SW-가시 취소채널 or 헤드 실GPU 무계누적 실증 시 비-취소 완화책. 진단 훅·게이트·영향 스크립트는 근거로 보존. build·build:lib·verify C1 회귀 0. ([이슈 #20](issues/20-cancel-backpressure-propagation.md))

### 2026-06-22
- [perf][docs] **실데이터(raw autzen 원본 10.65M·비정규화) 사후검증 + decode 병목 후속 이슈 #19 등록.** 정규화(1/5 decimation)·합성 점이 결과를 왜곡했는지 확인하려 원본을 직접 측정: 4축 비중이 norm과 거의 동일(**decode 84% · reproject 2%**, `docs/bench/axis-raw-autzen.*`) → decode 84% 병목은 입력 무관 견고. #17 격자 bilinear는 **실제 점 6M개**(합성 아님)로 재검증해 max 오차 **0.329mm**(합성 worst-case와 일치)·≈88×·격자 채택(`scripts/bench/check-reproject-realpts.ts`) — 합성 점 caveat 해소([#17](issues/17-reproject-proj4-internal-bottleneck.md) §5 갱신). 4축 산출물 이름을 입력 파일에서 유도하게 수정(`run-axis-profile.ts`; `axis-autzen-2M`→`axis-norm-autzen-2M` 분리, raw 덮어쓰기 방지). decode(laz-perf) 가속은 [이슈 #19](issues/19-decode-laz-internal-bottleneck.md)로 등록(Open). ([learn/08](learn/08-profiling-and-bottleneck-hunting.md) 실데이터 callout)
- [perf] **[이슈 #17] deep-load 내부병목 제거 — reproject(proj4 수평변환)가 내부 compute의 50%였음을 격자 bilinear로 54×↓ (50%→2%).** **발견**(4축 하니스): IO를 로컬로 통제하니 내부 계산의 **절반이 reproject(582 ms/1M점)** — 디코드(43%)보다 큼(정규화·raw autzen 일치, 2회 변동 0%p). **진단(measure-first)**: 격리 마이크로벤치(`scripts/bench/check-reproject.ts`)서 **V0(현재) 574 ≈ V1(배열 재사용) 564 → Δ2%뿐** → 비용은 JS 할당이 아니라 **proj4 투영 수학 자체**(Lambert 역투영 + NAD83→WGS84 데이텀, ~570ns/점) 확정 → 미세최적화로는 못 잡음. **BP 조사**(context7): proj4js 배치 API 부재(재사용 변환기는 이미 사용 중), 핵심 BP=**bounded-extent 근사** — COPC는 유한영역(autzen ~1km×1.4km)이고 conformal 투영은 소영역서 거의 선형 → **데이터셋 bounds 위 (G+1)² proj4 control 격자 1회 빌드 + 점별 bilinear 보간**으로 점당 proj4 호출 제거. **수정**: `src/copc-core.ts` `makeGridReprojector`/`GridReproj` 신설(`CopcSession.reproj`), `openCopc`·`decodeNode`·`loadCopcPoints`가 점별 `toWgs.forward` → `reproj.forward`. **정확성 가드**: 셀당 다점(중심+모서리 중점)서 proj4 대비 max오차 측정 → 임계(1mm) 초과 시 G 자동 상향(gridMax 64), 미달 시 **proj4 점별 폴백**(대형 extent/비정상 CRS 보호). **검증(RED→GREEN)**: reproject 582(50%)→**10.7(2%)** ms/1M점(하니스 e2e 54×), internal compute 2504→**1245ms(~2×)**, BOTTLENECK이 **decode(laz 84%)로 이동**, max오차 **0.33mm**(<1mm·lidar 정밀도 훨씬 아래), verify C1(center in Oregon) 좌표 동일·회귀 0(coalesce/retry/profile-axes/serve/tsc/build). dual-review #18 BLOCKED→가드 건전화(셀당 다점)+게이트 1mm+회귀 강화 후 머지(PR #18). 잔여 병목=decode(별건 후속). ([이슈 #17](issues/17-reproject-proj4-internal-bottleneck.md) · [learn/08](learn/08-profiling-and-bottleneck-hunting.md))
- [feat] **결정적 4축 병목 분해 하니스 — 내부 compute를 IO/decode/reproject/build로 반복가능하게 분리 측정 (PR #16).** **동기**(이슈 #14 후속): 기존 측정은 축 경계가 뭉개져(`decodeMs` 하나가 fetch+laz+reproject 한 덩어리) "어디가 느린가"를 못 가리킴. **설계**: 세 독립 부품 — ① PDAL 정규화 COPC(`scripts/bench/gen-norm-copc.sh`, decimation으로 점수·bounds·속성 고정; 무작위 합성은 LAZ 인접예측 압축 깨 decode 비현실이라 실데이터 기반) ② 로컬 정적 서버(네트워크 변동을 디스크 수준으로 제거) ③ Node 하니스(`scripts/bench/{axis-measure,profile-axes,run-axis-profile}.ts`, `npm run profile:axes`)가 **프로덕션 `src/` 무수정**으로 동일 프리미티브를 경계 타이머 호출. **결정성**: 고정 노드집합(depth≤D) + K회 median + 워밍업 제외 + **점수 정규화(ms/1M점)** → 축% 변동 0%p(2회 재현). **lazy decode 누수 함정**: reproject 측정 전 1차원 강제 materialize로 decode 비용을 decode 축에 귀속(인위적 지연 getter로 IO만 부풀려도 decode 불변 = AC #4 실증). **산출**: 4축 표(ms·%·ms/1M점)+BOTTLENECK 한 줄(JSON+md, `docs/bench/axis-<입력>.*`). **첫 발견 = reproject 50%**(→ 이슈 #17). (spec `docs/superpowers/specs/2026-06-22-deterministic-axis-profiler-design.md`)
- [docs] **학습 사이트 — 프로파일러·병목 사냥 챕터 신설(learn/08).** 이번에 만든 결정적 4축 프로파일러와 reproject 50%→2%(54×) 진단·해결 한 사이클을 학습용 서사로 정리: 왜 측정 도구가 필요했나(축 경계 뭉개짐)·도구 구조(3부품·축 격리·lazy decode 함정)·진단(V0≈V1로 처방 좁히기)·해결(bounded-extent 격자+가드)·검증(병목 이동). 배선: `learn/index` 08행·`learn/07` 푸터 next·`index.md` 더읽어보기·`PROFILING.md` 4축 하니스 섹션(실행 명령·`check-reproject`·08 포인터). (`docs/learn/08-profiling-and-bottleneck-hunting.md`)

### 2026-06-20
- [bench] **point budget 후 Eptium 재테스트 — 공정 비교 노이즈 장벽 해소 + 매칭 작동점서 ours GPU 우위(조건부).** 이슈 #08(pointBudget) 출하 후 같은 도구로 Eptium 재비교(PR #10·#11 + EDL-on). **(1) fair-compare 재실행**: ours가 2M 캡되니 영실험 노이즈 **62.9%→8.4%**(자기검증 처음 통과) — point budget이 노이즈 축을 풀었다. ours 깊은 줌 2M @ 7ms 정착(무제한 120ms→캡). 단 overlap 2/3(Eptium plateau 뷰포트 의존)로 단일 verdict는 여전히 거부. **(2) 매칭 예산 head-to-head**(`scripts/bench/probe-matched.ts`, ours cacheBytes를 Eptium 점수에 맞춤·고정 깊은 시점·정상상태 plateau): EDL-off raw-point서 ours 5.72ms(500k)↔Eptium 14.16ms(600k)=**ours GPU ~0.4×**. **(3) EDL-on 실사용 검증**(`--edl`, fair-probe `normalizeSurfaceEdlOn` 추가): EDL이 ours에 ~1ms 이하만 더함 → **우위가 EDL-off 아티팩트가 아님 확인**, 실사용서도 ours 5.19ms(500k)↔Eptium 9.08ms(400k)=ours ~1.75×. **⚠️ headline 금지([[competition-goal-north-star]] 준수)**: 매칭 작동점·단일 ds(sofi)·EDL 파라미터 미정합·**Eptium plateau run 변동(400~764k)** → 우위 방향은 견고하나 크기 1.75~2.5× 불확실. 방어 톤="양자화 경량 파이프라인의 측정 가능한 이득(매칭점 한정)". (`docs/bench/FINDINGS.md` §재재검증·§매칭·§EDL-on · `docs/bench/budget/matched-sofi{,-edl}.*`)
- [feat] **[이슈 #08] 깊은 줌 point budget — Cesium 네이티브 `cacheBytes` 메모리예산으로 렌더 점수 근사 캡 (손코딩 0).** **문제**(fair-bench 발견): ours 무제한 SSE refine이 깊은 줌서 점 폭주(sofi 9.29M)→GPU-bound(~16fps), Eptium은 764k 고정 점예산→120fps. **BP 조사**(deep-research+context7): Cesium은 점-개수 직접 캡 **부재**(`maximumScreenSpaceError`·`cacheBytes`(soft 바이트 예산)·`dynamicScreenSpaceError`만), Potree 계열은 노드-drop 우선순위 큐(기본 1~2M), **deck.gl Tile3DLayer는 우리처럼 점캡 없음**(동진영). EDL/atten on이면 "sub-pixel 여분점=공짜" 가정 위험(deep-research 경고). **measure-first 게이트**(`scripts/bench/probe-budget.ts`, 실 GPU M4 Pro): cacheBytes 스윕으로 ①품질 ②유계화 ③부드러움 측정 → **동적 SSE 루프 불필요, cacheBytes 네이티브로 충분** 확정(Simplicity·Circuit Breaker — 측정도구 결함 2회서 멈춤·증거종합). **구현**: `pointBudget` 옵션(기본 200만) → `cacheBytes=maximumCacheOverflowBytes=pointBudget×8B`(점당 실측 ~16B). Cesium `memoryAdjustedScreenSpaceError`가 한도 초과 시 refine 자동 억제(ADR-001 위임 철학 유지). **검증(실 GPU, 2시점×on/off)**: 깊은 줌 9.29M/61ms(16fps)→**2.10M/11ms(89fps)**, 정상뷰 7.0M/82ms→2.06M/34ms, **양 시점 스크린샷 시각 동일**(여분점=sub-pixel noise·오히려 speckle↓). 환산 정확(2M→실측 2.10M, ±5%). tsc·`verify`(autzen Oregon) 회귀 0. **measure-first가 전제 2개 적발(#05 교훈)**: ①sub-pixel 가정은 EDL on서도 참 ②"정상뷰 무영향" 거짓(msse=2서 7M)이나 7M→2M 시각동일이라 품질 OK. soft·근사(속성노출 시 점당바이트↑→실점수↓) JSDoc 명시. ([이슈 #08](issues/08-point-budget.md) · `docs/bench/budget/sofi-verify.*`+4스샷 · `src/copc-tileset.ts`·`scripts/bench/probe-budget.ts`)
- [feat/research] **공정 엔진 비교 도구(fair-compare) + point budget 약점 발견** (`feat/fair-engine-bench`, 미머지). **목적**: 우리 vs Eptium 성능을 *공정하게* 판정해 약점을 찾는 측정 도구. **도구**: `scripts/bench/fair-{compare,probe,types,report}.ts` — 5대 통제(동일 config 정규화·고정 시점·**GPU 타이머 쿼리 GPU ms**·로딩 곡선 샘플링·ours-vs-ours 영실험). **2번 피벗(스파이크/진단으로 확정)**: ① `--disable-gpu-vsync`가 macOS Metal 미작동(fps=65) → GPU 타이머로 cost 메트릭 전환. ② sofi 깊은뷰=단조 로딩 60s+(settle 비현실) → settle/이진탐색 폐기, 로딩 곡선 샘플링. **결과**: E2E(sofi)에서 도구가 자기검증으로 **verdict 거부**(`nullOk=false floor 62.9%·overlap=2 → 신뢰불가`, 가짜 숫자 0) — *단일 공정 verdict 불가*가 정직한 결론. **진짜 발견(measure-first)**: msse 스윕서 **Eptium 764k 고정(점 예산)** ↔ **ours 무제한 SSE refine(10M+)** → 깊은 줌 ours ~58fps↔Eptium ~120fps. **약점 = point budget 부재** → **이슈 #08**(GH #8, `docs/issues/08-point-budget.md`). §v4 "동급/2×" 주장은 712k 매칭 작동점 한정임을 `docs/bench/FINDINGS.md`에 기록(대회용 주장 가이드 포함). handoff `docs/handoff/fair-compare-and-point-budget-handoff.md`. (도구 한계: gpuMs 가파른 비선형→노이즈·점예산 격차→overlap 부족, 단일 verdict 산출은 큰 재설계 필요라 보류. `measureLoadCurve`는 self-프로파일러로 #08 검증에 재사용.)
- [docs] **용어 정규화 — "점군" → "포인트클라우드".** 문서 27개(CLAUDE.md·docs/ 학습·ADR·arch·superpowers 기록)의 "점군" 표기를 "포인트클라우드"로 일괄 치환, 한국어 조사 받침 보정(`점군을`→`포인트클라우드를`·`점군이`→`포인트클라우드가` 등). 결정 내용 무변경(append-only ADR도 용어만 정규화)·코드/README 영향 0. 사용자 선호 표기 확정.
- [feat] **[IMPROVEMENTS Tier1 #3-A] 점 피킹 정보 조회 — `pickPoint(tileset, scene, windowPosition)` free 함수 + 데모 패널.** 점 클릭→그 점의 정확한 경위도·고도(Cesium `scene.pickPosition`) + LAS 속성(#1 batch table `getProperty`)을 한 호출로 조회. 소유권은 `picked.primitive===tileset`로 판정(globe 관통·하늘·타 tileset→undefined). **렌더러 손코딩 0**(Cesium 내장 pick/pickPosition만)·신규 의존성 0. `PickedPoint{position?,cartographic?,featureId,attributes}` export. 데모 `demo/pick-panel.ts`가 클릭→우상단 패널(경위도·고도+속성). 검증: `check-picking` 4케이스(소유→위치+속성·타 primitive/하늘→undefined·pickPosition 미지원→degraded) 결정적 헤드리스 + autzen 브라우저 스모크(Oregon 경위도+Classification/Intensity 실값) + verify C1·기존 check-* 회귀 0. 범위: 옵션 B(옥트리 최근접점·측정 스냅)=별도 사이클. (spec/plan `docs/superpowers/2026-06-20-point-picking-*`) (`src/picking.ts`+`index`+`demo/pick-panel`+`README`)

### 2026-06-19
- [refactor] **[OSS Stage 1] 데모/랩 ↔ 라이브러리 분리 + 스파이크 프루닝.** 데모 하네스(`main`/`copc`/`datasets`)를 `demo/`로 이동, `src/`를 순수 출하 라이브러리 9파일로 고정. 디리스킹 스파이크(`?spike`~`?spike5`/`?spikeBatch`/`?spikeReal`) 프루닝 → `pnts.ts`·`spike-batch.ts` 삭제(git+docs/arch에 서사 보존), 스파이크 7모드 프루닝 → 데모는 기본 showcase + `?naive`/`?bench`/`?perf`/`?soak` 4쿼리모드. tsconfig `include`에 `demo` 추가(타입체크 커버리지 보존). **출하물 byte-identical**(dist 5파일 해시 불변·AC1), verify C1·check-*(ecef/coalesce/attributes/crs)·build·build:lib 회귀 0. 신규 의존성 0·공개 API 무변경. (spec/plan `docs/superpowers/`, Stage 2=copc-core 분할·Stage 3=영문화는 별도)
- [feat] **[IMPROVEMENTS Tier1 #2] CRS 자동배치 견고화 — no-WKT/파싱실패 fail-loud + `crs`/`defaultCrs` override.** **측정으로 갭 확정**: georef happy path(WKT projected)는 견고(WKT1·WKT2 모두 proj4 2.20.9가 처리 — 실측), 진짜 갭은 (1) **no-WKT**(copc.js `.wkt` undefined → x,y를 경위도로 → **조용히 지구 밖**, LAS 1.2~1.3 GeoTIFF-only 흔함) + (2) proj4 파싱실패·per-point out-of-domain **silent NaN**. **BP 조사**(proj4/copc.js 설치버전 실측 + prior-art 6종: py3dtiles·PDAL·giro3d·Potree·loaders.gl·Cesium) 합의 = **fail-loud + override(force vs fill-if-missing 2-mode, PDAL 패턴) + axis/center sanity 가드 + geoid scope-out**. **수정(A안)**: `resolveCrs`(우선순위 `crs`>wkt>`defaultCrs` + proj4 생성 try/catch) + `checkCenterInRange`(cube 중심 reproject 범위/NaN 검사)로 인라인 georef 3지점 통합, `crs`/`defaultCrs` 옵션을 `fromUrl`→페이지·워커 세션에 전달. WKT2는 proj4가 이미 처리해 **무작업**, per-point 배치 **무변경**(ECEF sub-mm 검증 유지). **검증**: `check-crs` 10/10(no-CRS throw·force override Δlon=-6°·fill-if-missing·garbage throw·center 범위밖/NaN throw) + autzen C1 회귀 0·ecef·hierarchy·coalesce·tsc·build PASS(**AC1~7**). **한계(정직)**: EPSG override는 proj4 내장분만(풀 레지스트리·GeoTIFF GeoKey 자동복구 = B안 follow-up)·geoid 미보정(web-viewer 업계 norm). (spec/plan `docs/superpowers/`, `src/copc-core.ts` `resolveCrs`/`checkCenterInRange`)

### 2026-06-18
- [fix] **[issue #04] coalescing in-flight/rebuild 레이스 — 잘린 슬라이스 → laz-perf WASM 폭증 수정 (#02 잠복 회귀).** 무거운 예제(sofi 1.9GB) Eptium 대조 테스트 중 발견: msse=4 극단 refine서 ours 가 laz-perf WASM 2GB abort + 타일 500. **측정으로 오진 정정** — "WASM 천장(볼륨 한계)"이 아니라 #02 coalescing 의 잠복 정확성 회귀. 근본: `createCoalescingGetter` in-flight dedup 이 `run.start` 로만 키잉 → 서브페이지 lazy 로드로 노드 수가 바뀌면 `rebuild` 가 run 경계 재계산("같은 start, 더 큰 end") → 진행 중인 옛(작은) region 을 새 run offset 으로 슬라이스 → 범위초과 **빈 바이트** → laz-perf 가 garbage 청크 헤더로 ~1.87GB alloc(워커 heap 7→1877MB 단일 점프). **coalesce OFF 는 더 무거운 부하도 heap 7MB 고정·완주** → 동시성+재계산 레이스 확정(범인 node 4-4-4-1, 단 21,743점). 수정(Apache Arrow `ReadRangeCache` 패턴): in-flight promise 를 fetch 한 region 의 실제 `{start,end,bytes}` 에 바인딩(C) + 슬라이스 전 커버리지 검사·미달 시 `base(begin,end)` 직접 폴백(B) → in-flight 경로가 이미-정확한 `cache.lookup` 과 동일 규칙. BP: Go `singleflight`(키=결과 정체성)·fsspec grouping 스냅샷. **검증 RED→GREEN(sofi msse=4 ON):** abort YES→0·슬라이스 범위초과 2→0·heap 1877MB→7.1MB 고정·4.21M정지→4.29M 완주(=OFF 동일). coalescing 이득 보존(msse=8 round-trip 10 불변). 회귀 0: 골든파일 byte-identical·신규 `check-coalesce` Task7(레이스 단위 결정적)·build·verify C1·repro-03(#03) PASS. ([이슈 #04](issues/04-lazperf-wasm-2gb-ceiling.md) · `coalescing-inflight-race` 위키 · `src/copc-core.ts` `createCoalescingGetter`)
- [perf] **[issue #02] range coalescing — deep-load round-trip 61→6·settle 13.9s→4.8s (Eptium 동급).** S3 HTTP/1.1 환경에서 deep-load 시간의 ~99%가 per-node range TTFB(0.65s×61req)임을 프로파일로 확인(이슈 #02 §6). 유일 레버=round-trip 수 감소. prior art: GDAL CPL_VSIL_CURL merge·fsspec `merge_ranges`·Zarr prefetch = 클라우드 최적 포맷 표준 기법; COPC-JS `decompressChunk` primitive를 활용해 인접 노드를 1 range fetch → 슬라이스 → 개별 decode. 설계: `createCoalescingGetter`(getter 데코레이터·in-flight 공유·passthrough) + `groupRuns`(two-cap: gap 256KB, size 8MB) + `createRegionCache`(총바이트 LRU 64MB). **실 S3·실 GPU(M4 Pro) 측정 — millsite msse=8 712k점:** round-trip 61→6(10×↓), settle 13892→4823ms(2.9×↓), pointsSelected 712458 동일(정확성 무변화), 골든파일 5노드 byte-identical, Eptium TTD ~4091ms 대비 4823ms(동급). coalesceMaxGap: 0 폴백은 기존 per-node 동작 동일. ADR-006 참조. ([ADR-006](adr/006-range-coalescing.md) · [이슈 #02](issues/02-deep-load-worker-pool.md) §8 · `src/copc-core.ts`)
- [bench] **매칭 점수 공정 비교 — 북극성 베이스라인 확보(millsite, 실 GPU).** #01·task#1·#03 수정 후 처음으로 **렌더 점 개수를 ±10%로 맞춘** 공정 비교 성립. 두 뷰어 refine 곡선이 같은 msse에선 안 겹쳐(coarse 영역 Eptium ~2×) scout(`scripts/bench/match-sweep.ts`, 뷰어당 1로드 in-page 스윕)로 매칭점 도출 → **ours msse=8(712k) ↔ eptium msse=14(757k, Δ+6.3%)**. bench에 per-target msse(`--msse-ours`/`--msse-eptium`)·settle 타임아웃(`--settle`)·`-matched` 리포트 추가. **결과(2회 범위, 동일 머신 M4 Pro Metal):** 부드러움 **동률**(양쪽 p50 8.3ms·120fps vsync·hitch 0 — "부드럽게" 상용 동급 달성, 이제 *적게 그려서*가 아님), 메모리 **ours ~2× 우위**(73.6 vs 144MB, 양자화 pnts 실이득), 로드 **Eptium ~4× 빠름**(3.8–4.1s vs 13.9–16.2s, deep-load IO=이슈#02). bytes 축은 여전히 SW-blind 무효. → "변환없이·부드럽게" 동급 입증·메모리 우위, "빠르게"(로드)가 유일 격차. (`docs/bench/FINDINGS.md` §v4, `scripts/bench/match-sweep.ts`+`compare-eptium.ts`, `docs/bench/eptium-millsite-matched.*`)
- [fix] **[issue #03] 빈 노드(0점·전부 노이즈) Model PROCESSING 영구 고착 → `tilesLoaded`/`allTilesLoaded` 무한대기 수정.** 벤치 settle 결함(task #1)의 근본원인. 측정(`scripts/bench/repro-03.ts`·probe `inspectTiles`): millsite msse=8에서 뷰 완성·pending=0 후에도 `numberOfTilesProcessing`이 **13에 영구 고착** → Cesium `tilesLoaded` 영영 false·`allTilesLoaded`/`initialTilesLoaded` 영영 미fire(provider 플러그인 소비자 **무한 대기** = [[no-silent-failures]] 위반). 고착 13개 전부 **depth=3 `.pnts`·`pointsLength:0`·`modelReady:false`** — ASPRS 노이즈(7·18, 기본 `hideClassifications`)만 있던 노드가 0점 디코드되는데도 `buildQuantizedPnts`가 **빈 pnts(POINTS_LENGTH:0)** 를 서빙 → Cesium 1.142가 `.pnts`→`Model3DTileContent`로 로드하나 **0점 Model 은 ready 도달 못 함**. **BP(Cesium 소스 ground truth)**: 204/200-빈바디는 `preprocess` throw→FAILED, 일반 tileset엔 빈-콘텐츠 ready 경로 없음 → **Cesium `missingTilePolicy`**(MVT 벡터 프로바이더가 빈 타일에 쓰는 그것) 채택 — 빈 노드를 **404**로 서빙하고 tileset에 `_runtimeContentCodec={missingTilePolicy:{statusCodes:[404]}}` 설치 → 404 reject→`isEmptyTile`→`Empty3DTileContent`(ready·에러 없음). `createContent` 미제공이라 일반 pnts(200) 무영향. 수정 4곳: worker `decode`(0점→null·missing→throw)·핸들러(null→`{empty}`)·SW(`{empty}`→404)·fromUrl(codec 설치). **검증(실 GPU)**: repro RED(tilesLoaded=false·proc=13)→GREEN(true·proc=0·READY 44→57), `build`·`verify` C1 PASS, ours pointsSelected **712,458 유지**(일반 렌더 무영향). private 필드 결합은 repro-03.ts가 회귀 가드. (`decode.worker.ts`+`copc-tileset.ts`+`copc-sw.js`, `docs/issues/03-tiles-processing-stuck.md`)
- [fix] **벤치 settle 메트릭 결함 수정 — 거짓 25s TTD → 실측 ~16s.** `scripts/compare-eptium.ts`의 `settleFullRes`가 `numberOfTilesProcessing===0`을 settle 게이트로 써서, 우리 SW-backed 타일셋에선 이 카운터가 **13에 영구 고착**(0으로 안 떨어짐) → settle 영영 미판정 → 루프가 25s 타임아웃 만료 → 거짓 TTD 25s. 진단(`scripts/bench/diag-settle.ts` 신규: raw stat 시간곡선): millsite msse=8에서 `pending→0 & tilesReady→44 & pointsSelected→712,458`이 ~14.6s에 모두 안정되는데 `processing`만 13 고착. settle 조건에서 `processing===0` 제거(`pending===0 && tilesReady 안정`만) → 재측정 TTD 16.4s(bench)/~14.6s(diag), settle이 타임아웃 아닌 안정-감지로 정상 종료. **부수 발견 → 이슈 #03 신규**: *왜* processing이 13에 영구 고착하는지(무한 대기/silent-failure vs benign deferred upload)는 별도 — Cesium `tilesLoaded`/`allTilesLoaded`가 영영 안 잡힐 수 있어 플러그인 API 결함 가능성([[no-silent-failures]] 정합). FINDINGS·이슈#02 §5 TTD 정정. (`scripts/compare-eptium.ts`+`scripts/bench/diag-settle.ts`, `docs/issues/03-tiles-processing-stuck.md`)
- [fix] **[issue #01] `CopcTileset` under-refine 수정 — geometricError base `spacing`→`cube_size/16`.** Eptium 벤치로 객관 측정 중 발견: 동일 데이터(millsite)·동일 octree·**동일 msse=8**에서 우리는 루트 1타일(40k점), Eptium은 109타일(1.49M점) **37×** 차. 원인=`src/tileset.ts` geometricError가 `spacing/2^d` 기반인데 COPC `spacing≈cube/147`이라 ept-tools 표준(`cube/16`)보다 **9.2× 과소** → SSE 9.2× 작아 같은 msse에 거의 refine 안 함(방향 정상, magnitude 오류). 수정: base를 `(cube[3]-cube[0])·zUnit/16`(ept-tools=Entwine/Eptium 혈통). **검증(실 GPU)**: millsite 40k→728k(타일 1→79), autzen 61k→1.46M, `npm run build` PASS. 잔여 deep-load 속도는 #02. (`src/tileset.ts`, `docs/issues/01-copc-under-refine-geometricerror.md`)
- [feat] **Eptium 성능 벤치 오라클 — 우리 플러그인 vs `viewer.copc.io` 대칭 측정 하니스.** Playwright(headed=실 GPU)+CDP로 양쪽을 `window.viewer`로 동일 카메라·동일 msse 구동해 `Cesium3DTileset.statistics`(렌더 점수·tilesReady·frametime·hitch) 비교. `--ds autzen|millsite|sofi`, `npm run bench:eptium`(prebench가 probe IIFE 번들 esbuild 재생성→`addInitScript` 주입; tsx `__name` footgun 우회). 결론(`docs/bench/FINDINGS.md`): autzen은 둘 다 vsync 천장이라 무의미·1a축(TTD/bytes)은 우리 SW 네트워크가 CDP에 안 잡혀 비대칭 → 유효비교=frametime·렌더점수. 이 벤치가 #01 under-refine을 적발. 설계/계획: `docs/superpowers/specs|plans/2026-06-18-eptium-bench-oracle*`. (`scripts/compare-eptium.ts`+`scripts/bench/*`)
- [docs] **[issue #02] deep-load 느림 진단 — 워커풀 측정 기각, 병목=네트워크 IO.** #01 후 잔여 격차(millsite ours ~6.5타일/s vs Eptium ~15). 가설(단일 디코드 워커 직렬화)을 손수 라운드로빈 풀로 구현→`?pool=1` vs `6` A/B(동일 뷰)→**966k/12s 동치**=디코드 병목 아님→**revert**(코드 무변경, circuit-breaker). 진짜 원인=HTTP/1.1 S3 ~6 동시연결(`pending=6` 고정; ADR-004 throttle 의도값). 부수발견: 벤치 settle 메트릭이 `numberOfTilesProcessing≠0`로 거짓 25s(실제 ~12s). 깊은 IO 프로파일은 다음 세션(`docs/handoff/eptium-bench-handoff.md`). (`docs/issues/02-deep-load-worker-pool.md`)
- [feat] **#1 속성·해상도 충실도 — per-point LAS 속성을 변환 없이 Cesium 동적 스타일링·피킹에 노출 (IMPROVEMENTS Tier1 #1, main 머지).** COPC 점의 LAS 속성(Classification·Intensity·ReturnNumber·NumberOfReturns…+extra-bytes)을 pnts **BATCH_TABLE+BATCH_ID**로 실어 Cesium에 전달 → 클라이언트가 `Cesium3DTileStyle`의 `${Classification}` 등으로 **동적 스타일링**, `Cesium3DTileFeature.getProperty()`로 **피킹 조회**. `attributes` 옵션(`undefined`=큐레이션 4 | `'all'`=extra-bytes 포함 | `string[]`; 없는 dim skip+warn) + `rampStyle(name,range)`/`tileset.attributeRange(name)` 헬퍼(공개 export). 변환 파이프라인이 속성·정밀도를 *소리없이* 드롭하는 갭(Ion 타일러=Intensity+Classification만; giro3d#633 extra-bytes=0)을 **무변환이라 구조적으로** 닫음 — 커뮤니티 리서치로 실수요 확인(Eptium $5k~25k·OSS 대체 전무). **프로세스**: brainstorm→PoC(BATCH_ID 필수 발견)→spec→plan(8 task TDD)→**서브에이전트 주도 실행**(구현+리뷰 task별)→**3중 리뷰**(opus whole-branch+Codex 적대+fix-delta, 전부 Ready). **검증**: Node 4종(check-attributes/pnts-batch/attr-pipeline/style)·verify C1·build·build:lib + **실 브라우저**(autzen Classification 동적 스타일·getProperty Classification/Intensity 실값·120fps). 신규 의존성 0·Cesium-free 경계 유지(attributes/pnts-quantized/copc-core/decode.worker)·기존 colorBy 회귀 0·no-batch byte-identical. **제약**: `${COLOR}` 폴백은 batch-table 포인트클라우드(Cesium Model 경로)서 미작동 → 구체색 사용(README 명시). ([ADR-005]; spec/plan = `docs/superpowers/{specs,plans}/2026-06-18-*`) (`attributes.ts`+`copc-style.ts`+`copc-core`+`decode.worker`+`pnts-quantized`+`copc-tileset`+`index`+`README`+`scripts/check-*`)
- [chore] **npm publish 연결 — `.env`(NPM_TOKEN) + 프로젝트 `.npmrc` env-expansion + `release` 스크립트.** 토큰은 `.env`(gitignore, `.env.example` 템플릿 동반)에만 보관하고 `.npmrc`는 `//registry.npmjs.org/:_authToken=${NPM_TOKEN}` **환경변수 참조**로 둬 시크릿 커밋 0 — `.npmrc`/`.env.example`은 커밋 안전. 배포는 `npm run release`(build:lib → `.env` 자동 로드 → `npm publish`) 한 줄. 검증 3단계: env 없이 `npm whoami`=E401(토큰 누수 0)·env 주입 시 whoami=`thissa`·`npm publish --dry-run`=`copc-cesium@0.1.0` 8파일(193kB) 패키징 OK. CI 자동 배포(GitHub 시크릿)는 외부 전송이라 보류(승인 게이트). (`.npmrc`+`.env.example`+`.gitignore`+`package.json`)
- [docs] **학습 사이트 drift 방지 — 핵심 코드 스니펫 src transclude + CI strict 빌드.** arch 페이지의 손-붙여넣기 스니펫 중 정확성이 중요하고 verbatim·변동성 높은 6개(`geometricError`·childKeys·buildNode·`maxSSE`·loadSubPage·`hideClassifications` 기본값)를 `pymdownx.snippets` 마커(`// --8<--`)로 **src 실코드를 빌드 때 인용** → 코드≠문서 drift 구조적 제거(나머지 ~25개는 큐레이션·함수 인용 원칙 유지). CI에 `docs` 잡(`mkdocs build --strict`) 추가 — 깨진 링크·앵커·nav + transclude 경로(`check_paths`) drift를 매 푸시 검출(파일 rename 시 빨강). 검증: 6개 live 인용 확인·`tsc` PASS(마커=주석, 동작 0변경)·strict 무경고. 잔여갭: 마커 region만 삭제 시 빈 블록(하드실패 아님). (`mkdocs.yml`+`src/{tileset,copc-tileset,copc-core}.ts`+`docs/arch/{01,04,05,06}`+`.github/workflows/ci.yml`)
- [fix] **`datasets.ts` 죽은 COPC URL 정리.** 2026-06-18 재확인서 millsite(data.entwine.io)·fema_pr·cahokia 가 404. millsite 는 hobu-lidar 버킷에 동명 파일 생존(206) → URL 교체(1.74→1.35GB 재빌드; `DATASETS[1]` 유지로 soak/perf 기본값 안전). fema_pr·cahokia 는 원본 회수·대체 미발견 → 제거(코드 참조 0; 과거 측정 기록은 RESULTS/PROGRESS 유지). 잔여 라이브 = autzen·millsite·sofi. tsc·build PASS. (`datasets.ts`)
- [feat] **per-host 동시성 옵션 `maxRequestsPerServer`(기본 6) — S3 타임아웃·재시도 폭풍 해소.** Cesium 호스트당 기본 동시성 18은 HTTP/2 가정값(v1.113에서 6→18 상향). S3 등 **HTTP/1.1 range 소스**(브라우저 호스트당 ~6 연결)엔 과해, 고밀도 스트리밍 시 8s 타임아웃·재시도 폭풍을 부른다 — 측정(고밀도 soak·MSSE2): 재시도 **85(타임아웃 58)**@기본18 vs **1**@maxReq6, 단 복원력 레이어가 흡수해 사용자 구멍(tileFailed)은 양쪽 0. 콘텐츠(`/__copc-real/`)는 앱 origin 서빙이므로 **그 호스트에만** `RequestScheduler.requestsByServer`로 상한(전역 `maximumRequestsPerServer` 미오염) → 동시 노드 디코드 → 다운스트림 range fetch 동시성 게이트. 기본 6 = 브라우저 HTTP/1.1 한도·iTowns 출하값·Cesium 1.113 이전 기본값. `0` 이하=Cesium 기본 유지, HTTP/2 CDN 뒤면 상향 가능. **BP 조사**(iTowns 6·Giro3D 10·Cesium 18(HTTP/2)·Potree 전역4·loaders.gl 전역64 — 대부분 재시도 0이라 우리 p-retry가 최견고)로 손코딩 primitive 0·Cesium 내장 메커니즘 사용 확정. 검증: 새 기본값 sofi 고밀도 soak 재시도 4(타임아웃 1)·실패 0, build·build:lib·verify C1 Oregon PASS. ([ADR-004] ③ 노브값 확정) (`copc-tileset.ts`+`README.md`)
- [docs] **학습 사이트 — "아키텍처: 코드로 읽는 설계" 트랙 신설 (`docs/arch/` index+00~06).** `CopcTileset.fromUrl()` 뒤의 설계를 큰그림→디테일로 집필(수동 md→html 변환이 아닌 적극적 교육 콘텐츠): 00 한 요청의 일생(시퀀스 ①~⑦)·01 공개 API와 동형성(옥트리↔3D Tiles·`GE=spacing/2^깊이`)·02 서비스워커 가로채기·03 워커 디코드(comlink·laz-perf·zero-copy)·04 LOD 위임(SSE)·05 hierarchy 페이징·06 상용 코어 4기둥. 매 페이지 mermaid 다이어그램 + 실제 `src/` 코드 인용 + ADR/wiki 교차링크. learn(개념)·wiki(깊은 메커니즘) 사이에 배치(`mkdocs.yml` nav). 설계 스펙 `docs/superpowers/specs/2026-06-17-architecture-track-design.md`. (독자=오너 본인, C++ 비교 없이 JS/TS 직접 설명) `mkdocs build --strict` 무경고·8페이지 mermaid 렌더 확인.
- [docs] **글 형식 — mini-sim 공부사이트 타이포 매칭 (`docs/stylesheets/extra.css`).** 본문 16px·line-height 1.65·본문폭 ~52rem·제목 위계(h1 1.9em+보더·h2 1.45em+옅은 보더·h3 1.2em, weight 600)·코드 0.85em. MkDocs Material 위 `extra_css` 오버라이드. (참조: `SimLab/mini-sim` `build_html.py` GitHub 스타일. 픽셀 단위 동일 룩은 파이썬 생성기 이식 필요 — handoff 참고)
- [fix] **MkDocs 한글 헤딩 앵커 깨짐 — 유니코드 slugify로 교체 (`mkdocs.yml`).** 기본 slugify가 비ASCII를 폐기해 한글 제목이 `#_1`,`#_2`가 되며 섹션 앵커 링크가 전부 깨지던 문제. `toc.slugify`를 `pymdownx.slugs.slugify(case=lower)`로 → 한글 앵커 보존 + mini-sim 파이썬 생성기(github_slugify)와 정렬돼 렌더러 교체에도 앵커 안정. arch 앵커·링크 불일치 0 확인.

### 2026-06-17
- [fix] **T1 "FAIL" 적대 리뷰 재검증 = false positive 확정 + 방어적 하드닝 2건.** Codex의 *"fromUrl 이 cold consumer 에서 `reg.update()` 에 hang → T1 FAIL"* 판정을 3중 측정으로 반증: dev cold(`installing` 상태·regs0·controller null)·**prod 빌드 static cold** 모두 `reg.update()` **1ms resolve**(Codex 빌드 자신의 콘솔 로그 `SW_REGISTERED 1130ms → SW_UPDATED 1131ms`), fromUrl 완료·**포인트클라우드 정상 렌더**(스크린샷 확인). 오탐 3종 규명: `nonBlackPixels:0`=WebGL drawingBuffer 읽기 아티팩트(preserveDrawingBuffer 없음), `api.cesium.com 401`=ion 토큰 없는 Viewer 기본 베이스맵(라이브러리 무관), `decode.worker.js 404`=직접 경로 오탐(실번들은 `/assets/decode.worker-*.js` 200). → **버그 아님 — "update 제거"를 버그 수정으로 밀지 않음**(band-aid 회피). 다만 방어적 2건 적용: ① **`reg.update()` 비차단화**(`void reg.update().catch()`) — timeout 없는 redundant await(`ensureServiceWorker` 의 유일한 무한대기 가능 지점) 제거, 실제 활성/제어 게이트는 register+ready+`waitForController`(8s)가 담당. 느린/단일스레드 정적서버에서 Cesium 번들 뒤 HOL-blocking 방어(방어적 개선, 버그 수정 아님 — [[no-silent-failures]] 무한대기 금지 정합). ② **Viewer `baseLayer:false`**(README quick-start + 데모는 토큰 없을 때만) — 심사위원 복붙 시 ion 401/검은지구본 **false-negative 차단**(smoke 오판의 진짜 원인). **검증**: dev cold 재현 fromUrl 완료(`contentRequests:7`·`ionRequests:0`·SW제어), `build`·`build:lib`·`verify` C1 Oregon PASS. (`copc-tileset.ts`+`README.md`+`main.ts`)
- [docs] **T1 마감 산출물 — README(영문 quick-start)·LICENSE(Apache-2.0)·CI.** README 재작성: 한 줄 포지셔닝("Stream COPC point clouds directly in CesiumJS — no conversion") + 설치 + **SW 복사 안내**(content 경로 덮는 scope 정직 명시) + quick-start + 옵션표 + 아키텍처(LOD/eviction Cesium 위임) + dev/배경(랩 docs 링크). LICENSE Apache-2.0. **의존성 라이선스 전부 permissive 확인**(cesium·laz-perf·comlink Apache-2.0 / copc·p-retry·proj4 MIT — laz-perf LGPL 우려 해소). GitHub Actions CI(`npm ci`→build→build:lib→verify) 추가. **남음(T1)**: 결과보고서 + 3분 시연 영상. (`README.md`+`LICENSE`+`.github/workflows/ci.yml`)
- [test] **T1 재현 게이트 PASS — 클린 소비자 앱에서 `copc-cesium` 동작.** 별도 Vite 앱(`/tmp/copc-smoke`)에 `npm pack` tarball 설치 → `import { CopcTileset } from 'copc-cesium'` → `fromUrl(autzen)` → **7타일 렌더·실패 0**(SW 가로채기·worker 디코드·wasm 로드·deps 전부 소비자 환경서 작동, 시각 확인까지). **풀어낸 패키징 난제 3종**: ① 출하 worker 가 소비자 *워커 컨텍스트*에서 bare import(comlink·laz-perf·copc) eval 실패(comlink 무응답→`fromUrl` hang) → **self-contained 번들**(tsup 2-빌드, worker `noExternal`) ② 번들된 copc→cross-fetch 가 node변종(node-fetch→punycode `require`)으로 해석 → **`platform:'browser'`** ③ npm `file:` tarball 같은-version 캐시로 stale 설치 → tarball 직접 추출로 우회. 회귀 0(`npm run build`→demo-dist·`verify` C1 Oregon PASS). **남음(T1)**: README quick-start + LICENSE. (`tsup.config.ts`)
- [chore] **`copc-cesium` 라이브러리 빌드 셋업 (tsup) — T1 패키징.** package.json 배포형 전환: name `copc-cesium`·`exports`/`types`·`peerDependencies:{cesium}`·`laz-perf` 직접 dep 승격(직접 import)·`files:[dist]`·`build:lib`. `tsup.config.ts`(엔트리 `index`+`decode.worker`, ESM+`.d.ts`, deps externalize). 데모(Vite) 출력은 `demo-dist/`로 분리 → `dist`=lib 전용. **dev/lib 분기 처리**: 소스는 Vite 친화(`?url`·worker `.ts`) 유지하고 *빌드 후처리 rewrite*로 `?url`→상대 `new URL('./laz-perf.wasm',…)`·worker `.ts`→`.js` + wasm·copc-sw.js dist 복사(esbuild 플러그인은 tsup dep-external 에 선점당해 후처리 채택). 검증: `build:lib` PASS·dist lib 전용·deps 전부 externalize. **남음**: 클린 앱 재현(npm pack→설치→fromUrl→렌더)=T1 합격 게이트. (`package.json`+`tsup.config.ts`+`vite.config.ts`+`tsconfig.json`)
- [feat] **T1 패키징 착수 — `copc-cesium` 라이브러리 진입점 + 서비스워커 견고성.** `src/index.ts` 공개 진입점(`CopcTileset` + `CopcTilesetOptions`·`ColorBy`). SW 하드코딩(`/copc-sw.js`) 제거 → `serviceWorkerUrl`/`serviceWorkerScope` 옵션. **견고성(Codex 적대 리뷰 4건 반영)**: ① content-coverage 가드(`CONTENT_BASE_PATH` 상수로 content URL 생성·scope 검증 일관) — 비-root scope의 *조용한 미가로챔*을 명확 에러로 표면화 ② `controllerchange` 무한대기 → 8s 타임아웃+명확 에러 ③ `?perf` longtask observer `buffered` 제거 + `addedAt` 필터(이전 longtask 오염 차단) ④ observer `finally` disconnect(예외 시 누수 차단). + `?perf`에 **하드웨어 무관 계측**(`longTaskMs`=메인스레드 freeze·`maxTilesPer16ms`=프레임 버스트) 추가. build·verify PASS. (`index.ts`+`copc-tileset.ts`+`main.ts`)
- [test] **지역단위 헤드리스 검증 — 노드 메타 누적 갭 = 누수 아님 확정 (C1·C2·C3·C5 PASS, C4=실 GPU 대기).** fema_pr·cahokia 로 측정. ① **georef(C1)**: FEMA PR −65.69°,18.38°(푸에르토리코)·Cahokia −90.06°,38.65°(일리노이) verify 일치, 8.9GB 도 ~5s root 디코드·soak 전구간 console error 0. ② **메타 천장(C3)**: Cahokia 90s soak `nodes` 2173→14352 후 **plateau**, heap ~90MB·cesiumMB 12MB·tilesReady 58 모두 plateau → 단일 파일 orbit/zoom 항해서 무한 누수 없음(전 범위 pan 으로 모든 서브페이지 훑을 때만 파일 총노드까지·세션 내 미해제 → LRU 는 그 시나리오 한정·별도 STOP 사이클). ③ **eviction(C2)**: cache=4MB→Cahokia `unloads 0→7`(Cesium 이 우리 SW-pnts 도 evict 재확인). ④ **회귀 0(C5)**: autzen verify C1 PASS·`tsc` OK. **발견**: Cahokia 화면 빈 영역 = MLS(차량 스캔) 회랑 커버 본질(extent ~3×3km 인데 8.9GB = 회랑 집중)이지 결함 아님 → 대회 "광역 균일 정합" 해자 샷은 FEMA PR(항공). 기준: `.claude-criteria.md`.
- [perf] **스트리밍 성능 추적 결론 — 파이프라인은 병목 아님, 30fps는 환경 throttle.** `?perf`에 격리 노브 추가(`edl`·`atten`·`res` + `?perf=globe`=점 없이 지구본만). **3중 격리로 우리 코드 무죄 확정**: autzen(초소형)==millsite(대형) 동일 fps · EDL/attenuation off 무변화 · 점 없는 globe-only도 동일. 결정타 — 순수 `requestAnimationFrame` 카운터가 about:blank에서 ~32Hz → **브라우저/전원 rAF 스로틀**(macOS 저전원·배터리, Playwright=swiftshader)이지 우리 렌더/Cesium 비용 아님. 유효 신호(hitch 0·TTD 0.2s·점 개수 무관) 양호. measure-first 가 *없는 버그* 추격을 차단(추측 최적화 0). 진짜 fps headline 은 비-스로틀(실 GPU·전원연결) 1회 측정만 남음. (`main.ts`)
- [feat] **대형 검증 데이터셋 2종 + `tileset.copcNodeCount()` 진단 — 노드 메타 누적 갭 계측 준비.** `datasets.ts`에 fema_pr(~980MB 항공·수평 광역)·cahokia(8.9GB 최대 단일 COPC) 추가. `CopcTileset.fromUrl` 반환 tileset에 `copcNodeCount()`(누적 옥트리 노드 메타 수 — 서브페이지 lazy 로드로 단조 증가·LRU 없음) 노출 → ADR-004가 "별개·미측정"으로 남긴 **노드 메타 누적 메모리 갭**을 8.9GB급에서 스트레스 측정할 훅. (`datasets.ts`+`copc-tileset.ts`)
- [feat] **스트리밍 성능 측정 하네스 `?perf` — frametime 분포·hitch·TTFP·TTD·메모리.** 결정적 카메라 경로(orbit + near↔far dive churn) 동안 frametime p50/p95/p99·hitch(>50ms)·TTFP·TTD(디테일 채우기)·peak메모리·tileUnload·평균 화면점수를 JSON 출력(`window.__perf`). `?perf[=ds]&secs=&maxReq=&cache=&msse=`. **측정 결과**: 기본 MSSE 8에서 millsite 파이프라인 건강(TTD 210ms·hitch 0·메모리 미미) → 헤드리스 기준 성능 문제 미검출; MSSE 2 강제 시 TTD 210ms→9.7s(부하 민감 축 = 디테일 채우기 속도). ⚠️ **Playwright frametime은 swiftshader(지구본 소프트렌더)라 fps 무효** — 유효 fps/부드러움은 실 GPU. **measure-first 준수**: 실 GPU 측정으로 문제 확인 전 최적화 보류(ADR-004 — 렌더러·LOD 손코딩 금지). 기준: `.claude-criteria.md`. (`main.ts`)
- [feat] **노이즈 classification 필터 — `hideClassifications` 기본 `[7,18]` (Potree 레퍼런스 대조).** 깊은 옥트리 millsite를 띄우니 지면 위에 **떠다니는 이상한 점 층**. 측정(`scripts/check-classification.ts`): **ASPRS class 18(high noise) 13%(루트노드 44%)** + 7, 평균 지면보다 ~415m 위·헤더 maxZ(2804)까지 오염. 독립 엔진 **Potree(로컬 `examples/copc.html`)도 동일 층 렌더 → class 18 숨기면 깨끗한 DEM** → 우리 버그 아닌 데이터 아웃라이어 확정. 측량 표준(PDAL·Potree·LAStools)대로 **디코드 시 classification 제외**(렌더·메모리·카운트에서 빠짐): `CopcTileset.fromUrl(url, { hideClassifications })`, 기본 노이즈 `[7,18]`, `[]`로 무필터. `decodeNode`/`colorize` 는 `keep` 인덱스로 위치·색·높이 정합 유지. **검증**: 헤드리스 루트 72,400→40,535(44% 드롭·배열 길이 정합), sofi는 노이즈 클래스 없음→무필터(보수적·데이터별 적응), 시각 millsite 떠다니는 점 0·120fps·실패0, verify C1 PASS. (`copc-core`+`decode.worker`+`copc-tileset`+`scripts/check-classification.ts`)
- [fix] **height 색을 전역 Z 범위로 정규화 — 노드별 무지개 얼룩 제거 (Potree `elevationRange` BP).** `colors.rampColors` 가 고도색을 **노드별 min/max**로 정규화해, 깊은 옥트리에서 refine된 작은 노드마다 자기 Z범위를 전체 무지개로 늘려 **평지에도 빨강~파랑 얼룩**이 생기던 결함(사용자 관찰). BP 조사: Potree `material.elevationRange` = 바운딩박스 전역 Z, 노드/타일 불문 **단일 범위**(millsite Potree 실측 `[1544,2804]`). 같은 방식으로 **COPC 헤더 전역 Z 범위**를 `colorize`→`heightColors`에 넘겨 정규화 + 범위밖 `[0,1]` 클램프 → 노드 간 색 일관. (`colors.ts`+`copc-core.colorize`)
- [feat] **데모 하네스 — `?ds=` 데이터셋 선택 + `?maxReq=` 동시성 throttle.** 기본 데모(`runDemo`)에 `?ds=autzen|millsite|sofi`(깊은 옥트리 시각 테스트) 추가 + soak에만 있던 `?maxReq=N`을 데모 경로에도 배선. millsite 실측: maxReq 기본 18→6 시 range 타임아웃 폭풍 **45→0**, 로드 8.1s→5.9s — ADR-004 ③(config 튜닝, 워커풀 아님)을 깊은 데이터셋에서 실증. (`main.ts`)
- [decision] **"부드럽게" 4모듈 탐색 결산 — 손코딩 스트리밍 primitive 전부 기각([ADR-004](adr/004-delegate-memory-concurrency-to-cesium.md)).** 주최사 목표어 "부드럽게"를 ①렌더 외형 ②점 예산 ③동시성 ④eviction으로 분해, STOP 규칙대로 Cesium 내장기능 확인+측정 선행. **②④ = Cesium `cacheBytes`+`memoryAdjustedScreenSpaceError`에 위임**(실측: `?soak=autzen&cache=2`에서 한도 초과 시 `tileUnload`로 우리 SW-pnts도 evict·메모리 plateau — 단 축출 대상은 *렌더 점 타일*, 서브페이지 *노드 메타* 누적은 별개·미측정·유효). **③ = 동시성-큐 타임아웃**(기본 18동시→S3 6/host 큐→8s 타임아웃에 큐대기 포함→헛실패; 같은 항해서 `maximumRequestsPerServer` 18→6시 재시도 184→23·500 4→0, 단 throughput↓ tradeoff) → 워커풀 아닌 **config 튜닝**. point-budget+LRU+워커풀 3종 불필요 확정, 신규 의존성 0. 노브값(maxReq·timeout·기본 cacheBytes)은 실 GPU 측정 게이트. (line 14 [decision] "측정 후" → 측정 완료. ADR-001 "메모리 캐시=우리 일" 가정 점 데이터 한해 정정.)
- [feat] **"부드럽게" ① 렌더 외형 기본값 — EDL·attenuation·colorBy 'rgb' 기본 on.** `CopcTileset.fromUrl` 기본값을 `eyeDomeLighting`·`attenuation` on + `colorBy` 'height'→'rgb'(차원 없으면 height 폴백)로. 옵션 없이도 데모가 무지개 점점이→사진급 RGB+EDL 입체(Cesium 지구본 위 product-grade)로 — Potree가 구조적으로 못 주는 "지구본 위 포인트클라우드" 해자 샷 확보. MSSE 8 유지(밀도/타임아웃은 ③ 영역이라 격리). 검증: autzen before/after, build PASS, 부작용 0(타임아웃 경고 0). (`copc-tileset.ts`)
- [feat] **측정 하네스 `?soak` — 메모리/항해 soak.** `?soak[=dsId]&cache=MB&maxReq=N&near=&far=&secs=` — 카메라 오실레이션 항해 중 `totalMemoryUsageInBytes`·JS heap·`tileUnload`·tilesReady 시계열 로깅(HUD+console). fps는 실 GPU만 유의하나 메모리 회계·동시성·실패는 헤드리스도 유의 → ②④ eviction 검증 + ③ 동시성 측정 도구. (`main.ts`; `RequestScheduler`/`HeadingPitchRange`/`Matrix4` import)
- [fix] **Step 0~3 종합 적대적 리뷰 반영(Codex).** (1) **SW 백스톱 15s→40s** — 워커 재시도 예산(fetch 8s×4시도+백오프 ≈ 34s)보다 짧아 *느린-회복* 읽기를 죽이던 문제(`copc-sw.js`). (2) **`fromUrl` 초기화 실패 정리** — `try/releaseSession/rethrow` 로 `activeSids`·워커 세션·SW 리스너 누수 차단(검증: 잘못된 URL→명확한 404 throw, 직후 정상 URL 200). (3) **`buildPageTileset` 잘못된 키 가드** — `loadSubPage` 실패+노드 부재 시 즉시 throw(지연된 `.pnts` 500 대신 표면화). (4) **colorBy 폴백 경고 세션당 1회**로 디노이즈(throw 아님 — 유효 점 렌더는 유지, 표면화는 console.warn). (5) 서브페이지 **무한 누적은 정직히 문서화**(LRU 측정 후 — Step1+ ③). `releaseSession` 헬퍼로 destroy·실패정리 DRY. build·verify·paging·retry PASS. (반박: colorBy 를 throw 로 바꾸자는 제안은 거절 — RGB 없다고 전체 타일셋을 깨는 건 과함.)
- [feat] **상용 코어 Step 3 — 속성 견고성 (colorBy 확장 + 색 로직 통합).** `colorBy` 를 `height`(기본)에 더해 `rgb`/`classification`(ASPRS 팔레트)/`intensity`(램프)/`returns`(팔레트)로 확장. 모든 색 로직을 신규 `src/colors.ts` 한 곳으로 통합(Codex #4 — `hslToRgb` 를 `pnts-quantized` 에서 이동; `pnts-quantized` 는 이제 위치 양자화+패킹만). `decodeNode` 는 모드별 차원(Classification/Intensity/ReturnNumber/RGB)을 읽어 색을 만들고, **차원이 없으면 height 로 폴백 + `console.warn`**(조용한 실패 X). pluggable = 직렬화 named 모드(커스텀 JS 함수는 projFunc 과 동일 IPC 사유로 미지원). **검증**: Autzen 5모드 **5/5 서로 다른 색**(class ground=[166,116,64]·return1=[230,25,75]); millsite(RGB 없음)+`rgb` → height 폴백·해시 동일·경고 표면화; verify·paging·build PASS. C1~C6 PASS. 신규 의존성 0. (`colors.ts`+`copc-core`+`pnts-quantized`+`decode.worker`+`copc-tileset`)
- [feat] **상용 코어 Step 2 — 네트워크 복원력 + 조용한 실패 제거.** copc.js getter 가 `response.ok` 검사·retry 없이 5xx/416 에러 바디를 점 데이터로 둔갑시키던 **조용한 실패**를 `httpGetterWithRetry`(copc-core)로 교체 — status 검사로 명확히 실패시키고, 일시적 실패(네트워크·타임아웃·429·5xx)는 **지수백오프+풀지터**(`p-retry`, retries 3·300→3000ms)로 재시도, 결정적 오류(4xx·416)는 즉시 중단. range fetch 마다 `AbortSignal.timeout(8s)`, SW→페이지 왕복엔 `Promise.race(15s)` 백스톱(무한 대기 차단 → 기존 catch→500). copc 의 3경로(header/hierarchy/point)가 모두 이 getter 를 쓰므로 한 곳에서 전부 커버. 부가: `void close()`→`.catch(log)` 등 fire-and-forget rejection 표면화. **검증**: `scripts/check-retry.ts` 8케이스 결정(일시실패→성공·404/416 즉시·503 소진·버그 TypeError 빠른실패·타임아웃→소진); 회귀 0(verify PASS·데모 tileLoad:10/fail:0). C1~C7 PASS. 신규 의존성 = p-retry(+is-network-error). **제외(오버엔지니어링)**: 서킷브레이커·요청 코얼레싱·Retry-After 완전파싱.
- [feat] **상용 코어 Step 1 — 하이어라키 서브페이지 페이징 (정확성 TOP 갭 해결).** `openCopc`가 `loadHierarchyPage`의 `{pages}`(미로드 자식 페이지 포인터)를 버리던 버그 → 보관. `pages[key]` 노드엔 **외부 tileset proxy 자식**(`content:page/{key}.json`)을 emit; Cesium이 refine 시 그 JSON을 요청하면 SW→페이지가 `loadSubPage`+`buildSubtree`로 child tileset 공급, 워커도 `loadPage`로 그 서브트리 노드를 로드해 `.pnts` 디코드 가능. 외부 tileset(3D Tiles 1.0, GE 경계 자동 합성) 방식 — implicit tiling 미사용(오버엔지니어링). SW는 JSON 라우팅만 추가(copc.js 미번들, ADR-002 유지). **측정**: Autzen 서브페이지 0(전부 렌더돼 "됐던" 것), millsite 141·sofi 111 무시됨 → 깊은 서브트리 미스트리밍이 정확성 갭이었음. **검증(결정적)**: Node — 서브페이지 노드 로드 전 미스/null → `loadSubPage` 후 88노드·23,359점 디코드(`scripts/check-paging.ts`); 브라우저 millsite — 동일 노드 `.pnts` `500→200`, `page/K.json` 200·유효 child tileset. C1~C6 PASS. 회귀 0(verify PASS). 신규 의존성 0. (`copc-core`+`tileset`+`decode.worker`+`copc-tileset`+`copc-sw.js`)
- [refactor] **상용 코어 Step 0 — 생명주기/destroy 소유권 (Codex 리뷰 최우선).** 워커에 `close(sid)` 추가 + `CopcTileset.fromUrl`이 돌려준 tileset의 `destroy()`를 확장(=`scene.primitives.remove()` 경유 시 자동 호출)해 워커 세션 정리. `activeSids` 추적으로 **마지막 세션이면 `worker.terminate()` + SW 메시지 리스너 제거**(전역 상태 누수 차단). 디코드 라우팅을 `decodeTile(sid,key)` 단일 함수로 캡슐화(워커풀의 좁은 seam). *wrapper 클래스·공개 추상화 없음(Codex 가이드).* 검증(SW-서빙 상태로 결정적 판정): per-session close(`s1:500`/`s2:200`), 재생성(`s2After:200`·새 워커엔 `s1:500`), remove 에러·렌더크래시 0, build·verify PASS. C1~C6 PASS. (`copc-tileset.ts`+`decode.worker.ts`)
- [feat] **본편 ④ options — `CopcTileset.fromUrl(url, opts)` 렌더 옵션.** `pointSize`(Cesium3DTileStyle), `attenuation`·`eyeDomeLighting`(pointCloudShading, EDL은 attenuation 동반 — 거친 LOD 단차 가림), `colorBy:'height'|'rgb'`(워커가 COPC Red/Green/Blue 디멘션 읽음, 16-bit면 >>8, 없으면 height 자동 폴백). 검증: Autzen `hasRgb:true`, 'rgb' vs 'height' 색 바이트 상이·점수 동일(61201)·둘 다 양자화, `tileLoad:6/fail:0`, 기본(옵션 없음) 회귀 없음, build·verify PASS. C1~C6 PASS. 신규 의존성 0. (`copc-tileset.ts`+`decode.worker.ts`+`copc-core.decodeNode`(wantRgb)+`pnts-quantized`(rgb))
- [decision] **`projFunc`(JS 함수) 드롭 — 레퍼런스 검증 완료.** 디코드/reproject가 워커 per-point 핫루프라 함수를 comlink로 넘기면 점마다 async IPC 왕복(호출이 Promise화) → 마비. 검증: prior art는 **누구도 워커에 per-point 함수를 안 넘기고 직렬화 CRS 문자열**을 씀(Giro3D `registerCRS(code, proj4문자열)`·Potree proj4 문자열·deck.gl 선언적 CRS·proj4 `defs`). **정정**: ADR이 본보기로 든 TIFFImageryProvider의 `projFunc`는 워커를 넘나드는 per-point 함수가 아니라 **메인스레드 1회 `(code)=>transform` 팩토리**였음(워커 per-point 워크로드엔 부적합한 결론은 동일). 오버라이드 필요시 `sourceCrs?: string`(또는 워커-사이드 팩토리)로 후속. 좌표계는 파일 WKT로 자동 처리(현재 충분). ADR-001 §4·각주 정정.
- [feat] **본편 ③-A 성능 — 디코드를 Web Worker로 이동(comlink) + POSITION_QUANTIZED 컴팩트 버퍼.** 메인스레드 inline 디코드(laz-perf WASM + proj4 + pnts 빌드)를 `src/decode.worker.ts`로 분리(`copc-tileset.ts`는 `comlink`로 위임). 디코드 세션은 워커가, tileset.json 지오메트리는 페이지가 보관(둘 다 `openCopc`, 병렬). pnts는 `src/pnts-quantized.ts`에서 Cesium 없이 생성 — uint16×3 양자화로 위치 바이트 12→6(점당 15→9B, −40%), RTC_CENTER + per-tile QUANTIZED_VOLUME. 검증: 기본 데모 `tileLoad:10/fail:0`·콘솔에러0, `npm run verify` PASS(Autzen), ECEF 공식 Cesium 대비 1.4e-9m 일치(`scripts/check-ecef.ts`). C1~C6 PASS.
- [decision] **③의 LRU·워커풀은 보류(측정 후).** BP 조사 결과 Cesium이 이미 `cacheBytes`로 타일 바이트 상한·축출 → 앱 레벨 디코드 캐시는 중복 가능성(Giro3D조차 COPC 캐싱 TODO). 단일 워커로 시작, 재디코드/디코드 큐잉이 관측되면 `lru-cache`·`workerpool`로(손코딩 금지). STOP 규칙 준수: 계획+검증기준 승인 후 착수. 신규 의존성 = comlink 1개.

### 2026-06-16
- [chore] 프로젝트 스캐폴드 — Vite + CesiumJS + copc.js + 4축 프로파일링 하네스 (Phase 0). 실브라우저 부팅 검증.
- [feat] Phase 1 갭 실증 — naive COPC→Cesium baseline + 헤드리스 `verify`/`sweep`/`?bench`. C1 정확성(georef=Autzen), C2 naive 벽(~2~3M점), C3 재현 PASS. **벽의 원인은 GPU가 아니라 `PointPrimitiveCollection` 점당 오버헤드(메모리 1KB/점)** — 측정으로 확인.
- [docs] 문제정의·범위(PROBLEM) + 경쟁전략·계단식 목표(STRATEGY) + prior art 갭 검증(REFERENCES). "오픈+Cesium+COPC" 조립품 부재 확정 (동명 GitHub repo는 코드 0줄 빈 스텁).
- [docs] ADR-001 — 결과물 = Cesium provider 플러그인(`fromUrl`), 아키텍처 A(COPC 옥트리를 동적 Cesium3DTileset으로, LOD는 Cesium 위임). 근거: 과제의 TIFFImageryProvider(COG) 유사 힌트 + 측정.
- [feat] Phase 2 스파이크 — 런타임 pnts→Cesium3DTileset 다리 확정(`src/pnts.ts`, `?spike`); Cesium은 content를 XHR로 요청 → **서비스워커**가 가로채 온디맨드 pnts 응답(`public/copc-sw.js`, `?spike2`/`?spike3`). 아키텍처 끝에서 끝까지 디리스킹.
- [docs] 학습 사이트(MkDocs Material) — learn/ 6장 + 친절 개요 랜딩 + mermaid 다이어그램. (참고: mini-sim study 스타일)
- [feat] Phase 2 본편 ① — 진짜 COPC 노드를 서비스워커 경로로 온디맨드 렌더(`?spike4`). SW가 페이지로 라우팅(MessageChannel)→페이지 copc.js 디코드. stale SW 제어권 race는 unregister 선행으로 수정.
- [feat] Phase 2 본편 ② — **옥트리 LOD 스트리밍 작동**(`?spike5`). `copc-core.openCopc/decodeNode` + `tileset.ts`(옥트리→region tileset 트리). Cesium SSE가 278노드 중 **24노드만** 선택→온디맨드 디코드(`tileLoad:24/fail:0`). "보는 만큼만"이 Cesium 주도로 실데이터에서 구현됨.
- [feat] **`CopcTileset.fromUrl()` 공개 API + 기본 데모**(`src/copc-tileset.ts`). spike5 로직을 라이브러리 형태로 추출(세션별 sid 라우팅). 기본 페이지가 변환 없이 LOD 스트리밍 데모(`tileLoad:5/fail:0`). 결과물이 ADR-001의 `fromUrl` 형태를 갖춤. 남음: Web Worker 디코드·캐시·options·README.
- [fix] 타일 LOD 경계 단차 완화 — boundingVolume 높이를 큐브 대신 **실제 데이터 Z 범위로 조임**(SSE 정확도↑ → 이웃 타일 refine 일관성↑). 점 감쇠·EDL은 단차 *진단*을 가려서 보류(④에서 옵션화). MSSE 기본 8. (사용자 관찰: 인접 타일 LOD 단차 — 일부는 octree LOD 본질 특성)
