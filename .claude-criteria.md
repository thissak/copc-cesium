# Acceptance Criteria
<!-- task: Step 0 상용코어 선정리 — 생명주기/destroy 소유권 + 디코드 라우팅 캡슐화 (Codex #1·#3) -->
<!-- created: 2026-06-17 -->
<!-- 범위: 저위험 refactor. 공개 추상화/wrapper 클래스 금지(Codex 가이드). 워커풀·LRU는 후속. -->

- [x] C1 정리: `primitives.remove(ts)`→내 destroy 오버라이드(close+cleanup), 에러 0·렌더 크래시 0(`removeErr:null`, `isDestroyed:true`). ※ destroy는 씬에서 제거 후(=remove 경유)가 정석
- [x] C2 라우팅 캡슐화: SW 핸들러가 `decodeTile(sid,key)` 경유 — 타일 서빙 200/8타일 로드
- [x] C3 per-session close + 생존: 1개 제거 시 그 sid만 닫힘(`s1:500`), 다른 세션 계속 서빙(`s2:200`)
- [x] C4 재생성: 전체 teardown(terminate+리스너제거) 후 fromUrl → 재생성 워커가 서빙(`s2After:200`), 새 워커엔 옛 세션 없음(`s1After:500` = 깨끗한 teardown)
- [x] C5 회귀 없음: `npm run build`·`npm run verify`(Autzen) PASS. (tileLoad 카운트는 헤드리스 렌더 타이밍상 흔들려 SW-서빙 200/500으로 결정적 판정)
- [x] C6 오버엔지니어링 0: wrapper/플러그인 없이 Cesium `tileset.destroy()` 확장만. 신규 의존성 0

## 테스트 시나리오
- 정상: fromUrl→add→destroy → 세션 정리, 마지막이면 worker terminate, 에러 0
- 엣지: 2 tileset → 1 destroy(나머지 렌더 지속) → 2 destroy(terminate+리스너 제거) → 재 fromUrl(재생성 렌더)
- 실패: destroy 2회 → Cesium 표준 가드대로. 디코드 라우팅 실패는 기존대로 SW error
<!-- eval-loop:  -->
<!-- evaluation: PASS -->
