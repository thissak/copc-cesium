# Acceptance Criteria
<!-- task: 상용 코어 Step 1 — 하이어라키 서브페이지 페이징 (외부 tileset, 정확성 TOP 갭) -->
<!-- created: 2026-06-17 -->
<!-- 측정: Autzen 서브페이지 0(전부 렌더), millsite 141·sofi 111 무시됨(깊은 서브트리 미스트리밍). -->
<!-- 해법: 외부 tileset(3D Tiles 1.0) page-proxy 자식 + SW 온디맨드. implicit tiling 미사용. ADR-001/002 정합. -->

- [x] C1 `openCopc`가 `{nodes,pages}` 보관 + `buildNode`가 `s.pages[ck]` 자식에 page-proxy(외부 tileset) emit, 그 외 재귀
- [x] C2 `page/{key}.json` 온디맨드: 브라우저 `page_status:200`·`page_valid:true`(root.content=K.pnts)·childCount:6; 워커 loadPage 후 `after_pnts:200`
- [x] C3 깊이 확장(핵심): Node 체크 — 서브페이지 노드 로드 전 미스/null → `loadSubPage` 후 88노드·**23,359점 디코드**. 브라우저 — 동일 노드 `.pnts` `500→200`. 페이징 전엔 완전 미스
- [x] C4 회귀: `npm run verify` PASS(Autzen). Autzen pages 0 → proxy 0 → tileset 동일(by construction). K 점은 외부 root에서만(proxy는 content=json, 중복 없음)
- [x] C5 생명주기: destroy 시 `pageSessions.delete(sid)` + 워커 close + (idle 시) terminate. Step 0 경로 통합
- [x] C6 `npm run build` 통과, 신규 의존성 0, SW는 JSON 라우팅만(copc.js 미번들 유지), implicit tiling 미사용

## 테스트 시나리오
- 정상: millsite 서브페이지 키 → `page/K.json` 200·유효 tileset → 깊은 노드 `.pnts` 디코드 200
- 엣지: Autzen → proxy 자식 0개 → 기존 동일; 페이지 재요청 → 추가 fetch 0(세션 캐시)
- 실패: 잘못된 page key → loadSubPage 실패 → SW 500, 앱 미충돌
- 결정적 검증: Node `check-hierarchy`/신규 스크립트로 root→sub-page 로드→깊은 노드 디코드(점>0); 브라우저 SW-서빙 status(page/K.json=200, 깊은 .pnts=200)
<!-- evaluation: PASS -->
