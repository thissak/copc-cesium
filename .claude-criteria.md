# Acceptance Criteria
<!-- task: copc-cesium 라이브러리 패키징 (DIRECTION §8 T1 — "외부 심사위원이 새 환경에서 실행 가능") -->
<!-- created: 2026-06-17 -->

- [x] `npm run build:lib` (tsup) → `dist/` (ESM + `decode.worker` 별도 엔트리 + `.d.ts`), tsc 통과
- [x] 라이브러리 표면만 빌드 — `src/index.ts`; `copc.ts`·`main.ts`(naive/spike/데모) 제외
- [x] `copc-sw.js`가 패키지 `files`(dist)에 포함
- [x] **재현(핵심) PASS** — 깨끗한 Vite 앱(`/tmp/copc-smoke`)에 `npm pack` tarball → `import { CopcTileset } from 'copc-cesium'` → `fromUrl(autzen)` → **7타일 렌더·실패 0·SW 가로채기·worker 디코드** (우리 머신 밖)
- [x] laz-perf WASM: `?url` 제거, worker self-contained + `new URL('./laz-perf.wasm', import.meta.url)` + dist 복사 → **실제 디코드 성공으로 검증**(7타일)
- [x] `serviceWorkerUrl`/`serviceWorkerScope` 옵션 + content-coverage 가드(비-root scope 명확 에러)
- [x] `cesium` peerDependency 번들 제외; **worker self-contained(noExternal)+`platform:'browser'`** (소비자 워커 bare-import eval 실패·cross-fetch node변종 punycode 회피)
- [x] 기존 데모(`npm run build`→demo-dist)·`npm run verify`(C1 Oregon) 회귀 0
- [ ] README: install → `copc-sw.js` 복사(content 경로 덮는 scope·기본 root) → `fromUrl` 예제 → 동작; 의존성별 라이선스 + LICENSE 파일

## 테스트 시나리오
- [x] 정상: 클린 Vite 앱 + tarball → autzen 7타일 (worker 디코드+wasm+SW 동작)
- 엣지: `copc-sw.js` 미복사 → `ensureServiceWorker` 명확 에러 (미테스트)
- 엣지: SW가 content 경로 미커버 scope → content-coverage 가드 throw
- 실패: `cesium` 미설치(peer) → 명확한 안내 (미테스트)

## 학습 (패키징)
- npm `file:` tarball 은 같은 version 캐시 → 반복 테스트는 tarball 직접 추출로 우회.
- 출하 worker 는 self-contained 필수 + `platform:'browser'`.
- 남은 T1: README quick-start + LICENSE(Apache-2.0) + 의존성 라이선스.
