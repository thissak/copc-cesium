# Acceptance Criteria
<!-- task: 상용 코어 Step 2 — 네트워크 복원력 (range 재시도 + 타임아웃) + 조용한 실패 제거 -->
<!-- created: 2026-06-17 -->
<!-- BP: copc getter 는 retry·response.ok 검사 없음(5xx 바디를 점 데이터로 둔갑 = 조용한 실패). p-retry 사용. -->
<!-- 제외(오버엔지니어링): 서킷브레이커·코얼레싱·retry버짓·Retry-After 완전파싱·워커풀. -->

- [x] C1 getter 교체: `openCopc`·`loadCopcPoints`가 `httpGetterWithRetry` 사용 → copc 3경로 전부 커버 (verify·build·데모로 확인)
- [x] C2 status 검사+분류(조용한 실패 제거): non-ok 명확 throw(잠복버그 수정); 404·416 즉시실패(calls=1), 429/5xx·네트워크 재시도 — `check-retry.ts` 결정 검증
- [x] C3 backoff: retries 3·factor 2·300→3000ms·풀지터. 일시실패→성공(calls=3)·지속 503 소진(calls=4)·정상(calls=1) PASS
- [x] C4 타임아웃: fetch `AbortSignal.timeout` — 무응답 fetch가 타임아웃→재시도→소진 후 표면화(timedOut, calls=4). SW 왕복 `race(15s)`→catch→500(코드). 무한 대기 없음
- [x] C5 회귀: `npm run verify` PASS, 기본 데모 `tileLoaded:10/fail:0`·콘솔에러0, build 통과
- [x] C6 오버엔지니어링 0: p-retry(+is-network-error)만. 서킷브레이커·코얼레싱·Retry-After 없음
- [x] C7 조용한 실패 제거: copc의 response.ok 미검사(5xx→점데이터 둔갑) 수정; `void close()`→`.catch(log)`; 버그 TypeError는 빠르게 실패(calls=1, 4번 마스킹 X); SW 무한대기→15s 표면화

## 테스트 시나리오
- 정상: 네트워크 OK → 0 재시도, 기존과 동일
- 재시도: 일시적 5xx/네트워크 실패 N회 후 성공(mock) → 데이터 반환, onFailedAttempt 경고
- 즉시실패: 404·416 → 재시도 0, 명확한 에러 throw
- 타임아웃: 무응답 fetch → 8s abort 후 재시도; 멈춘 SW 핸들러 → 15s → 500(tileFailed, 무한대기 X)
