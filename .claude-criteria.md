# Acceptance Criteria
<!-- task: Phase 2 스파이크 — COPC 노드를 런타임 생성 pnts로 Cesium3DTileset에 먹이기 (다리 존재 증명) -->
<!-- created: 2026-06-16 -->
<!-- 직전 Phase 1(갭 실증) 기준은 전부 PASS, docs/RESULTS.md·PROGRESS.md에 기록됨 -->

- [x] **S1 PASS** — 런타임 pnts(data URI)를 `Cesium3DTileset`이 로드: `tileLoad:1, tileFailed:0`. 다리 존재 확정
- [x] **S2 PASS** — `RTC_CENTER` 적용, center -123.069°,44.056° = Autzen Oregon (시각 jitter 정밀확인은 실 GPU에서)
- [x] **S3 PASS** — 헤드리스 콘솔 `SPIKE RESULT` JSON으로 검증 (points 50000, pntsBytes 750184)
- [x] **S4 PASS (온디맨드 가로채기)** — Cesium은 content를 XHR로 요청(spike2 진단) → **서비스워커**가 XHR 가로채 요청 시점 pnts 생성·응답, `tileLoad:1/fail:0` (spike3). 진짜 스트리밍 메커니즘 확정.

## Phase 2 본편 ① — 진짜 COPC via 서비스워커 (spike4)
- [x] **S5 PASS** — SW가 진짜 Autzen 노드를 온디맨드 응답 `swAsked:1, tileLoad:1, fail:0` (SW→페이지 MessageChannel)
- [x] **S6 PASS** — center -123.069°,44.056° = Autzen, 실데이터 50k점
- [x] **S7 PASS** — 헤드리스 `SPIKE4 RESULT`
> 설계: 디코드는 페이지(추후 Web Worker), SW는 가로채기+라우팅. copc.js를 SW에 번들하지 않음.
> 수정: stale SW 제어권 race → 기존 SW unregister 선행 + clients.matchAll 폴백으로 해결.

## 테스트 시나리오
- **정상**: COPC 루트 노드 → pnts 변환 → Cesium 렌더, center ≈ -123.07, 44.06
- **실패**: Cesium이 pnts/tileset 거부(`tileFailed`>0 또는 throw) → 다리 방식 재검토(data URI→blob→서비스워커). **그것도 결정적 정보.**

## 판정
- **PASS(S1~S3)** → 다리 확정 → Phase 2 본 스트리밍(옥트리 전체 + 동적 공급) 계획 착수
- **FAIL** → 접근 재검토 (blob / 서비스워커 / 커스텀 Resource)
