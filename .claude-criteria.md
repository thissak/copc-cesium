# Acceptance Criteria
<!-- task: 상용 코어 Step 3 — 속성 견고성 (colorBy 확장 + 색 로직 통합) -->
<!-- created: 2026-06-17 -->
<!-- colorBy: height|rgb|classification|intensity|returns. 색 로직 colors.ts 단일 소유(Codex #4). -->
<!-- pluggable = 직렬화 named 모드; 커스텀 JS 함수 미지원(projFunc 과 동일 IPC 사유). -->

- [x] C1 5개 모드 동작: Autzen 에서 height/rgb/classification/intensity/returns **5/5 서로 다른 색**(distinctColorings 5). 팔레트 정확(class ground=[166,116,64], return1=[230,25,75])
- [x] C2 견고성(폴백): millsite(RGB 없음)+colorBy:'rgb' → height 와 **동일 해시**(폴백) + 콘솔 warning `colorBy 'rgb' 차원 없음 → height 폴백`(copc-core:220, 워커→브라우저 표면화)
- [x] C3 색 통합: 색 로직 전부 `colors.ts`; `pnts-quantized`는 위치 양자화+패킹만(hslToRgb 이동, build 통과)
- [x] C4 회귀: `npm run verify` PASS, height/rgb 기존과 동일(rgb [174,172,147]·height [0,100,255]), build·paging PASS
- [x] C5 직렬화 유지: colorBy 는 문자열(워커 전달). 커스텀 JS 함수 미지원(projFunc 동일 사유)
- [x] C6 신규 의존성 0

## 테스트 시나리오
- 정상: classification/intensity/returns → 색 변경(height 와 바이트 다름), tileLoad>0/fail:0
- 엣지: 차원 없는 파일+해당 모드 → height 폴백 + warn (에러 없음)
- 실패: 잘못된 colorBy 문자열은 TS 타입이 차단
<!-- evaluation: PASS -->
