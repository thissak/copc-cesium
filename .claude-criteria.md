# Acceptance Criteria
<!-- task: ④ 마감 options — pointSize/attenuation/eyeDomeLighting/colorBy (projFunc 드롭) -->
<!-- created: 2026-06-17 -->
<!-- 이전: ③-A PASS(PROGRESS·CHANGELOG 기록). projFunc는 per-point IPC 마비로 드롭 결정. -->

- [x] C1 pointSize: `{pointSize:5}` → `tileset.style` 적용 확인(`styleApplied:true`)
- [x] C2 attenuation/eyeDomeLighting: `{attenuation,eyeDomeLighting:true}` → `pointCloudShading.attenuation:true·eyeDomeLighting:true`, 콘솔 에러 0
- [x] C3 colorBy: Autzen RGB 존재(`hasRgb:true`). 'rgb'(s2) vs 'height'(s1) root 색 바이트 다름(`colorsDiffer:true`), 점수 동일(61201), 둘 다 양자화. 폴백은 decodeNode가 rgb 없을 때 자동 height
- [x] C4 회귀 없음: 옵션 없는 기본 데모 정상 로드, `npm run verify` PASS(Autzen)
- [x] C5 build 통과(tsc+vite), 신규 의존성 0 (comlink 외 추가 없음)
- [x] C6 projFunc 드롭: 인터페이스 부재 + CHANGELOG/handoff에 per-point IPC 사유 기록

## 테스트 시나리오
- 정상: fromUrl(url, {pointSize:3, attenuation:true, eyeDomeLighting:true, colorBy:'rgb'}) → 큰 점·EDL·실색, tileLoad>0/fail:0
- 엣지: RGB 디멘션 없는 COPC + colorBy:'rgb' → height 램프로 자동 폴백(에러 없음); 옵션 {} → 기존 동일
- 실패: 잘못된 pointSize 타입 등은 TS가 차단 (런타임 입력 없음 — 라이브러리 API)
<!-- eval-loop:  -->
<!-- eval-loop: 2 -->
<!-- eval-loop: 3 -->
