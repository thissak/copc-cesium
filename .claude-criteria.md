# Acceptance Criteria
<!-- task: copc-cesium 라이브러리 패키징 (DIRECTION §8 T1 — "외부 심사위원이 새 환경에서 실행 가능") -->
<!-- created: 2026-06-17 -->
<!-- 직전 태스크(지역단위 실증)는 완료 — C1~C3·C5 PASS, C4(부드러움)도 M4 Pro cahokia 런(p99 9.7ms·180s 0 hitch)으로 PASS. 결과는 PROGRESS 반영. -->

- [ ] `npm run build:lib` (tsup) → `dist/` (ESM + `decode.worker` 별도 엔트리 + `.d.ts`) 생성, tsc 통과
- [ ] 라이브러리 표면만 빌드 — `src/index.ts`(→`copc-tileset`→`copc-core`·`tileset`·`colors`·`pnts-quantized`·`decode.worker`). `copc.ts`·`main.ts`(naive/spike/데모)는 제외
- [ ] `copc-sw.js`가 패키지 `files`에 포함되어 배포물에 들어감
- [ ] **재현(핵심)**: 깨끗한 임시 앱에 `npm pack` tarball 설치 → `import { CopcTileset } from 'copc-cesium'` → `fromUrl()` → autzen 렌더 (우리 머신 밖)
- [ ] laz-perf WASM: Vite 전용 `?url` 제거. `locateFile`(이미 존재) 유지하되 wasm URL을 번들러-무관하게 — **dist에 `laz-perf.wasm` 복사 + worker에서 `new URL('./laz-perf.wasm', import.meta.url)` 상대 resolve**(bare-specifier 아님). **클린 앱 실제 디코드 성공으로 검증**
- [ ] `serviceWorkerUrl` 옵션 (기본 `/copc-sw.js`); 미복사/미설정 시 명확한 에러
- [ ] `cesium`은 peerDependency로 번들 제외 (배포물 cesium 중복 0)
- [ ] 기존 데모(`npm run dev`)·`npm run verify` 회귀 0
- [ ] README: install → `copc-sw.js` 복사(**content 경로를 덮는 scope, 기본 root** 명시) → `fromUrl` 예제 → 동작; 의존성별 라이선스 + LICENSE 파일

## 테스트 시나리오
- 정상: 클린 Vite 앱 + tarball → autzen 뜸 (worker 디코드 + wasm 로드 + SW 가로채기 전부 동작)
- 엣지: `copc-sw.js` 미복사 → `ensureServiceWorker` 명확 에러
- 엣지: SW가 content 경로 미커버 scope → `/__copc-real/` 미가로챔 → README root-scope 요구로 방어
- 실패: `cesium` 미설치(peer) → 명확한 안내
<!-- eval-loop:  -->
<!-- eval-loop: 2 -->
<!-- eval-loop: 3 -->
